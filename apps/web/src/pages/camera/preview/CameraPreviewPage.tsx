import { PagePlaceholder } from "../../../shared/ui/page-placeholder/PagePlaceholder";

export function CameraPreviewPage() {
  return <PagePlaceholder pageName="CameraPreviewPage" route="/camera/preview" />;
}
