/**
 * 필기 캔버스 렌더링(화면 표시용 `HandwritingCanvas`, 내보내기용 `exportStrokesToPngBlob`) 공용 상수.
 * 두 곳에서 동일한 값을 각자 하드코딩하면 어긋날 수 있어 한 곳에서만 정의한다.
 */
export const PEN_SIZE = 3;
export const ERASER_SIZE = 24;
/** Figma `label/primary`와 동일한 잉크 색(토큰: `--color-label-primary`). */
export const INK_COLOR = "#232b38";
