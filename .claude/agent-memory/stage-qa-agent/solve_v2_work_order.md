---
name: solve-v2-work-order
description: Solve v2.0 (WORK/DIAG/RESUME) 8-step work order location, stage naming (4a-1/4a-2/4b/4b-정정 1차+2차 all verified PASS), a recurring duplicated-type risk, QA techniques for verifying additive-only diffs and test rewrites, and a HIGH-severity "새 문제 풀기" navigation race found via full E2E click-through
metadata:
  type: project
---

`docs/FRONTEND_IMPLEMENTATION_PLAN.md` §1.3.1 defines an 8-step work order for the PRD v2.0 diagnostic-tutor Solve rebuild: 1 ActionBar state machine, 2 mock UI components (WorkLineList/DiagnosisCard/ResumeModeBar/ResumeResultCard, hardcoded mock data only), 3 WorkLineEditor (was blocked on Figma, later unblocked with owner-approved placeholder tokens), 4 backend wiring for WORK/DIAG, 5 backend wiring for RESUME, 6 HandwritingHighlightOverlay, 7 CHAT context expansion, 8 METHOD screen. Progress log lives in `docs/PROJECT_STATUS.md` §3.16 (plan) / §3.17 (post-hoc design-agent audit of steps 1-2, found and fixed 2 P0 + 2 P1 Figma-measurement defects).

Step 4 was further split by the owner into sub-stages: **4a-1** = shared-types (`WorkLine`/`CasStepVerification`/`Diagnosis`) + validation zod schemas + `stubCasVerification` (always `isValid: true`, deterministic placeholder until a real CAS/SymPy service exists) + new Express routers (`work.router.ts` POST `/api/problems/:problemId/work-lines`, `diagnosis.router.ts` POST `/api/problems/:problemId/diagnose`) + `FakeLLMAdapter`/`OpenAIAdapter` interface methods (`recognizeWork`/`diagnose`, OpenAI side stub-throws) + frontend API clients/hooks (`recognizeWork.ts`/`diagnoseProblem.ts`, `useRecognizeWork`/`useDiagnose`) owned by `ProblemInputProvider` — explicitly **no screen wiring yet**. Verified 2026-09-05: all of this landed cleanly, gate (typecheck/lint/test/build) green, test counts api 282/web 371, zero diff in every file the owner listed as regression-sensitive (ActionBar, WorkLineList, DiagnosisCard, ResumeModeBar, ResumeResultCard, WorkLineEditor, the 3 app-level flow tests, and the 5 untouched api modules). 4a-2 (real OpenAI implementation) and 4b (actually wiring the hooks into WORK/DIAG screens) are the expected next sub-stages — recheck those file lists again when QA-ing them, they will change.

**Recurring risk to recheck in future stages**: this codebase intentionally keeps *some* mock-UI-local types deliberately distinct from the new shared-types domain types (e.g. `WorkLineList.tsx`'s local `WorkLine` uses `isValid`/judgment-badge semantics, on purpose different from `shared-types.WorkLine`'s `isLowConfidence`/recognition-confidence semantics — confirmed not accidentally mixed as of 2026-09-05). But `DiagnosisCard.tsx`'s local `Diagnosis` interface (from step 2, still mock-only) is a **byte-for-byte structural duplicate** (same 7 fields, same JSDoc) of the new `shared-types.Diagnosis` added in 4a-1 — this was out of scope to fix in 4a-1 (screen wiring is 4b) and isn't a regression, but flag it again as a dedupe candidate when 4b wires DiagnosisCard to the real hook — if it's still a separate local interface at that point instead of importing `shared-types.Diagnosis`, that's a genuine "중복 선언" violation of `.claude/rules/frontend.md`.

**Why:** the owner explicitly worried about exactly this kind of type confusion (asked me to grep for WorkLine mixing) — worth extending the same suspicion to `Diagnosis` next time since the shapes are identical.
**How to apply:** whenever QA-ing a Solve v2.0 sub-stage, `grep -rn "interface WorkLine\b\|interface Diagnosis\b\|interface CasStepVerification\b"` across `apps/web/src` and `packages/shared-types/src` to catch new duplicates, and check whether components eligible to import the shared type still hand-roll a local one after backend wiring lands.

**4a-2 (real OpenAIAdapter `recognizeWork`/`diagnose`) — STAGE PASS, verified 2026-09-05.** Scope was strictly `openai-adapter.ts` (+`WORK_LINES_JSON_SCHEMA`/`DIAGNOSIS_JSON_SCHEMA`/`buildDiagnoseUserMessage`, 2 new methods), `prompts/system.ts` (+`buildRecognizeWorkPrompt`/`buildDiagnosePrompt`), `openai-adapter.test.ts` (+6 mocked tests, zero network). Confirmed via `diff` against `git show HEAD:<file>` (not just `git diff`, since 4a-1 was still uncommitted and sat in the same working tree) that all 4 pre-existing adapter methods and all 4 pre-existing prompt functions are byte-for-byte unchanged — pure appends only. Gate: api 288/288 (282 baseline + 6 new), web 371/371 (unchanged), typecheck/lint/build all green.

**Independently re-ran a live OpenAI smoke test myself** (not just trusted the developer's self-report) for all 4 `diagnose` scenarios the developer said they manually verified (오류형/중단형/DIAG-5/DIAG-6), using a throwaway `tsx --env-file=apps/api/.env` script that dynamically imports the real `OpenAIAdapter` class by absolute path (works because tsx's loader hook is path-independent; `.mts` extension needed to allow top-level `await`, plain `.ts` fails with "top-level await not supported in cjs"). All 4 passed with correct shapes; DIAG-6 in particular led with answer-acknowledgment before critique as the strengthened prompt requires, and the other 3 scenarios showed no regression from that prompt edit. One soft note (not a blocker): my synthetic DIAG-5 input text wasn't ambiguous enough to trigger `isLowConfidence:true` in the response — `diagnose()` operates on already-recognized text, not the image, so a true low-confidence-recognition trigger needs a genuinely illegible source image, which a text-only smoke script can't reproduce.

**Why:** the orchestrator flagged that the developer's own re-verification of a mid-course prompt fix (DIAG-6) shouldn't be taken on faith. **How to apply:** when a stage's live-smoke-test claim is central to the PASS decision, don't just read the code — reproduce it independently with a real (cheap, non-image) API call if credentials are available in `apps/api/.env`, especially for prompt-only changes where unit tests can't catch behavioral regressions (mocked tests only verify wiring/parsing, not actual model behavior).

**4b (mock components wired into real INPUT/WORK screens + "문제 인식하기" flow pivot) — STAGE PASS, verified 2026-09-05.** This sub-stage went through 3 rounds of post-hoc design-agent audit before reaching QA (P0: missing "풀이 결과" header/새 문제 badge on DIAG result screen; P1×3: missing recognized-problem summary bar, "내 풀이"→"내가 쓴 풀이" typo, unbounded left-column height risk of overlapping ActionBar; then a 2nd-round P0: DIAG result screen had zero follow-up-chat ChatFooter/suggested-questions — `diagnose()` wasn't calling `fetchSuggestedQuestions` and the page had no `<ChatFooter>` at all; then a 3rd-round finding that `hashtags={diagnosis.relatedConcepts}` duplicated `DiagnosisCard` content and didn't match Figma's fixed-preset-question footer pattern, fixed by emptying to `hashtags={[]}` with a "결정 필요" comment). All of that was already fixed by the time this stage-qa run started — verified fresh, not just trusted: `ProblemInputProvider.submitProblem`/`resumeFromHistory` are byte-for-byte unchanged (confirmed via `git diff 1851bfa -- <file> | grep '^-'` returning nothing but the diff header — zero deletions anywhere in the file, pure additive `recognizeOnly`/`giveUp`/`diagnose`-wrapper/second `useDrawingStrokes()` instance for `workStrokes`); the pre-existing `isResultReady && solveResult ? <ResultPanel>` branch in `SolveLandscapePage.tsx` has zero diff around it (confirmed by isolating a diff hunk anchored on that exact line); `routing.test.tsx` has zero diff. The `DiagnosisCard.tsx` local `Diagnosis` dedupe flagged as backlog in 4a-1 **was in fact resolved in 4b** (now `import type { Diagnosis } from "shared-types"`) — but `docs/FRONTEND_IMPLEMENTATION_PLAN.md` §7's backlog row for it is now stale (still describes it as open); flag for the orchestrator to mark resolved when writing the §3.20 log entry. `apps/web/src/shared/lib/solve/deriveWorkLineJudgments.ts` (the `shared/` importing `features/` violation the orchestrator self-fixed before this stage) is clean on re-check — imports only from `shared-types`, defines its own local `WorkLineJudgment` (deliberately not reusing `WorkLineList.tsx`'s local `WorkLine` name, with a comment explaining the structural-typing reason). No backend files changed in 4b at all (`git diff 1851bfa --stat` over `apps/api`/`packages/shared-types`/`packages/validation` shows exactly the same file set as the already-reviewed 4a-1+4a-2 diff, and api test count stayed at 288 — confirms 4b stayed frontend-only as scoped). Gate: typecheck/lint green, api 288/288 (unchanged), web 379/379 (371 baseline + exactly 8 new: 3 in `ActionBar.test.tsx` for the WORK-stage `isBusy` mutual-exclusion cases + RESULT-stage-via-diagnose case, 5 spread across `problemInputFlow.test.tsx`/`DiagnosisCard.test.tsx` import-fix/`cameraFlow.test.tsx` — net new `it()` blocks, not just modified existing ones), build green (pre-existing >500kB chunk warning only, not new).

**Test-rewrite verification technique used**: rather than trusting the developer's claim of "8 of 11 tests in `problemInputFlow.test.tsx` rewritten, 3 unchanged", ran `git diff 1851bfa -- <file>` directly and counted hunks — confirmed exactly 8 `it()` bodies touched (new `recognizeThenGiveUp()` helper replacing single "문제 인식하기" clicks, since v2.0 now requires a second "아직 못 풀겠어요" click to reach the old solve-result behavior) and exactly 3 fully untouched (`/solve/landscape` direct-entry redirect, MyPage "다시 풀기" resume, resume-failure). Same technique applied to `cameraFlow.test.tsx` (1 of 4 rewritten, matched exactly).

**Regression-test technique for a specific "stroke persists across a Provider-owned second canvas" claim**: wrote a *temporary* e2e test (not committed, deleted immediately after running) mocking `recognizeWork` to reject, drawing a stroke on the WORK-stage canvas, triggering the failure, dismissing the error modal, then clicking the eraser-tool-select button (not "취소"/clear!) purely to force a re-render without wiping strokes, and asserting the mock canvas context's fill-call-count stays > 0 throughout. This is the right pattern for "did X survive an error + rerender" claims when no existing test covers it: add a throwaway test under `apps/web/src/app/__qa_*.test.tsx`, run it once via `pnpm --filter web test -- --run <path>`, then `rm` it — never leave QA-authored test files in the tree (that would be QA modifying product test suite, which is against the role's core rule).

**4b 정정 1차 실행 (ActionBar 4-way state table + `RecognizedChip`/`EmptyStateHint` + `startNewProblem()`) — STAGE CONDITIONAL PASS, verified 2026-09-06.** Baseline for `git diff` is still `1851bfa` (unchanged since 4a-1 — it remains the tip of the last *committed* history; everything from step-3 WorkLineEditor through this stage sits uncommitted in the same working tree, so `git diff 1851bfa` always shows the full cumulative diff, not just the current sub-stage). To isolate what THIS sub-stage actually touched, cross-reference file mtimes (`stat -f "%Sm %N" <file>`) against the diff content: files modified 2026-09-05 (WorkLineEditor.tsx, WorkLineList.tsx, DiagnosisCard.tsx, all of apps/api, packages/validation, packages/shared-types) are 4a-1/4a-2/4b leftovers already verified in earlier entries above — confirm by reading their diff hunks match the *content* those earlier entries described (e.g. DiagnosisCard.tsx's diff should be exactly the `Diagnosis` de-dupe import, WorkLineEditor/WorkLineList's diff should be exactly the "내 풀이"→"내가 쓴 풀이" typo fix). Files with 2026-09-06 mtimes are the real current-stage diff. This mtime-vs-content cross-reference is the technique to reuse whenever multiple approved sub-stages sit uncommitted in one working tree and the task says "confirm file X has no diff" — that instruction means "no diff beyond what earlier approved sub-stages already introduced," not literally zero vs the last commit.

Verified correct: the 4-way stage table in `actionBarState.ts`/`ActionBar.tsx` matches the owner's table exactly (input/work-notyet/work-done/result, checked by direct code reading against the table in `docs/PROJECT_STATUS.md` §3.21); `ActionBar.test.tsx` rewritten to 12 tests covering all 4 stages + 3 `isBusy` mutual-exclusion cases, all pass; `RecognizedChip.tsx` has the corrected `gap-[8px] px-[14px] py-[7px]` + inset highlight shadow + new `Badge` `size="recognized-chip"` (`rounded-[10px] px-[7px] py-[2px] text-[11px] font-[590]`) from the design-agent's 2nd-round fix instruction; `ActionBar.tsx` segments share one `font-[590]` uniformly (base style only, never overridden); `SolvePencilcanvasPage.tsx` container has `gap-[11px]`. `ProblemInputProvider.submitProblem`/`resumeFromHistory`/`beginReinput`/`giveUp` confirmed byte-identical (zero `-` lines in `git diff 1851bfa -- ProblemInputProvider.tsx` except the diff header) — new `startNewProblem()` is pure-additive and only wired to one `onClick` handler (`SolveLandscapePage.tsx`'s `onNewProblem`), never auto-invoked, so no handwriting-loss regression risk.

**Bug found via a reproducible throwaway test (technique above), not caught by design-agent's Figma-compliance review**: `ActionBar`'s new RESULT-stage "새 문제 풀기" segment only has a working `onClick` on `/solve/landscape` (`SolveLandscapePage.tsx` passes `onNewProblem`). `/solve/pencilcanvas`'s `<ActionBar>` invocation (`SolvePencilcanvasPage.tsx`) never passes `onNewProblem` at all. `RequireProblemInputGuard` allows staying on/returning to `/solve/pencilcanvas` whenever `problemId !== null` regardless of `solveStatus`/`diagnoseStatus`, so a user who reaches RESULT then navigates back to `/solve/pencilcanvas` (browser back, or any `router.navigate` back to that path) sees the "새 문제 풀기" segment rendered enabled+emphasized (since `ActionBar`'s own `deriveStage` only checks `solveStatus`/`diagnoseStatus`, not the route) but clicking it is a silent no-op (`onClick={isResultStage ? onNewProblem : onRecognize}` → `undefined`). No crash, just a dead end — MEDIUM severity. Root cause: the RESULT segment's click-handler wiring was added only where the owner's example snippet showed it (`SolveLandscapePage.tsx`), and nobody re-checked the sibling page that renders the same shared `<ActionBar>`. **How to apply next time**: whenever a shared component gains a new prop/callback for a specific stage, grep all of that component's call sites (not just the one mentioned in the plan doc) to confirm the new prop is wired everywhere the stage is reachable.

**4b 정정 1차 실행 — 재검증 (fix-of-fix), STAGE PASS, verified 2026-09-06.** Scope: exactly 2 files — `SolvePencilcanvasPage.tsx` (destructure `startNewProblem` + wire `onNewProblem={() => { startNewProblem(); navigate("/solve/pencilcanvas"); }}` on its `<ActionBar>`) and `SolveLandscapePage.tsx` (stale "448px" comment corrected to "543px", matching the actual `w-[543px]` on that same line — the `w-[448px]` that still exists in `SolvePencilcanvasPage.tsx` is a separate, correct, unrelated value for that page's own container, not a leftover). Confirmed scope via mtime cross-reference (`stat -f "%Sm %N"`): only these 2 files had mtimes newer than every other uncommitted file in the tree, and `find ... -newermt` across `apps`/`packages`/`docs` (excluding `dist/`) returned only these 2 source files. Gate: typecheck/lint green, api 288/288, web 380/380 (both unchanged from pre-fix baseline — this was a wiring fix, not a new-test-adding stage), build green (same pre-existing >500kB chunk warning).

**Reproduction technique pitfall worth remembering**: my first attempt at reproducing the exact MEDIUM scenario (reach RESULT via `giveUp()`/solve → manually `router.navigate("/solve/pencilcanvas")` to simulate "browser back" → click "새 문제 풀기") produced a false alarm — the app appeared to redirect to `/camera` after the click, looking like a *new* regression. Root cause was in my own test harness, not the product: calling `await router.navigate(...)` directly on a `createMemoryRouter` instance outside of `act()` triggers React's "An update to RouterProvider inside a test was not wrapped in act(...)" warning, and the resulting out-of-band commit left the app in an inconsistent timing state by the time the click fired. Fix: wrap both the manual back-navigation and the click (plus a `setTimeout(resolve, 0)` flush for the `void navigate(...)` inside the app's own click handler) in `await act(async () => { ... })`. After that, the test passed cleanly and consistently, confirming the real fix works (INPUT stage correctly restored, `startNewProblem()` actually invoked). **How to apply next time**: when a throwaway QA repro test needs to simulate "browser back" against a `createMemoryRouter`, always drive it through `router.navigate(...)` wrapped in `act()` (or better, an actual UI element the user would click) — never trust a first failing result from a manually-driven router transition without first ruling out an act()-boundary artifact in the test itself, especially when the failure looks like a *totally unrelated screen* (e.g. redirecting to `/camera` when nothing in the diff touches camera/guard code).

**4b 정정 2차 실행 ("개념설명" 해시태그 + "관련개념" 카드 + `Diagnosis` 백엔드 확장) — final QA verified 2026-09-06.** Scope isolated via mtime cross-reference (files with `stat -f "%Sm %N"` >= 2026-09-06 13:04, distinct from the 1차 files which cluster at 02:xx/11:56/12:11 that same day): `packages/shared-types/src/index.ts` (+`ConceptExplanation`, `Diagnosis.conceptExplanations: ConceptExplanation[]`), `packages/validation/src/index.ts` (matching zod schema), `apps/api/.../{prompts/system.ts,openai-adapter.ts,openai-adapter.test.ts,fake-adapter.ts}` (prompt instructs LLM to generate `conceptExplanations` directly — confirmed via reading `buildDiagnosePrompt` and `DIAGNOSIS_JSON_SCHEMA`, no `curriculum_nodes`/Supabase join anywhere in `diagnose()`), `ResultCard.tsx`+test (optional `title` prop, additive, 2 existing call sites don't pass it so old 2-tier layout is unchanged), `SolveLandscapePage.tsx` (concept-card render block placed after suggestedQuestions/chatMessages/chatStatus, immediately before `<div ref={chatEndRef}/>` — matches Figma "Body 최하단" requirement; `isConceptCardVisible` reset added inside the exact same `if (isPanelVisible !== prevIsPanelVisible) { if (isPanelVisible) { ... } }` block as the pre-existing `panelWidth` reset), `docs/COMPONENT_MAP.md` (Result Card row now correctly documents LLM-direct-generation, no DB join), plus purely-additive test fixture updates (`conceptExplanations: []`) in `DiagnosisCard.test.tsx`/`deriveWorkLineJudgments.test.ts`/`useDiagnose.test.tsx`/`diagnoseProblem.test.ts` to satisfy the widened `Diagnosis` type, and a new describe block in `problemInputFlow.test.tsx` (`recognizeDrawWorkThenDiagnose` helper + "#개념설명" toggle test) confirmed via `git diff 1851bfa` hunk-by-hunk to be pure addition beyond the already-verified 1차 `recognizeThenGiveUp` rewrites. Gate: typecheck/lint green, api 288/288 (unchanged), web 383/383 (380 baseline + 3 new: 2 `ResultCard` title tests + 1 concept-toggle E2E test), build green (same pre-existing >500kB chunk warning). All 2차-specific requirements PASS.

**HIGH finding discovered via own throwaway E2E test, NOT caught by any existing test (including the original 1차 stage-qa PASS) — "새 문제 풀기" (New Problem) button on `/solve/landscape`'s RESULT stage navigates to `/camera` instead of the intended `/solve/pencilcanvas`, for BOTH the solve-result and diagnosis-result paths.** Root cause: `SolveLandscapePage.tsx`'s `onNewProblem={() => { startNewProblem(); navigate("/solve/pencilcanvas"); }}` calls `startNewProblem()` first, which synchronously resets `problemId`/`recognizeStatus`/`capturedImage`/`strokes` etc. React 18 batches these into one re-render, but react-router's `navigate()` (even for a loader-less path change on a data router) resolves asynchronously — so React commits a render where `RequireProblemInputGuard` (still mounted, wrapping `/solve/landscape`) sees `isAllowed === false` (since `hasProblemInput`/`problemId` are now cleared) and its own `useEffect` fires `navigate("/camera", { replace: true })`. This second, later-queued navigation appears to consistently win the race over the handler's original `navigate("/solve/pencilcanvas")`, both in `createMemoryRouter` tests and (since the mechanism is React/react-router state timing, not a jsdom artifact) very likely in a real browser too. Confirmed reproducible on the plain solve-result (WORK-4 `giveUp()`) path with **zero diagnosis-related code involved** — i.e. this is a pre-existing regression in 1차's `onNewProblem` wiring (`SolveLandscapePage.tsx`), not something 2차 introduced, that simply had no full-app E2E coverage until now (the only prior tests were `ActionBar.test.tsx`'s isolated unit test with a mocked `onNewProblem`, and the 4b-정정-1차 fix's own repro test which exercised the *other* `onNewProblem` — the one in `SolvePencilcanvasPage.tsx`'s own ActionBar for the browser-back scenario, which is unguarded and thus never hits this race). **How to apply next time**: whenever a click handler calls both a state-resetting function AND `navigate()` in the same synchronous handler, and the *source* route is wrapped by a guard that redirects based on that same state, write a full-app E2E test (real `RouterProvider` + real guard, not an isolated component test) that asserts the *final* `router.state.location.pathname`, not just that some UI text appeared — an isolated `ActionBar.test.tsx`-style test with a mocked callback cannot catch a guard race, no matter how many assertions it has.

**Technique note**: reproduced by capturing the `router` instance returned from `createMemoryRouter` (return it alongside `render()`'s result from a local `renderApp` helper) and asserting `router.state.location.pathname` directly, plus `--reporter=verbose` was needed for `console.log` to actually print (default vitest reporter suppresses stdout for passing tests) — run via `npx vitest run --reporter=verbose <path>` from `apps/web/`, not the root `pnpm test` script (which doesn't forward `--reporter` cleanly through the recursive workspace runner and also runs the whole suite regardless of a path argument passed after `--`).

**HIGH fix verified — final QA of 4b 정정 (1차+2차) combined, STAGE PASS, verified 2026-09-06.** Fix: `ProblemInputProvider.startNewProblem()` now calls `setIsRequestingReinput(true)` as its very first line (before any reset), reusing the exact same guard-bypass flag `beginReinput()` already used — so `RequireProblemInputGuard`'s `isAllowed` check stays true across the state-clearing render, closing the race with `navigate()`. `RequireProblemInputGuard.tsx` itself has zero diff (confirmed via `git diff -- <file>` against HEAD, empty). Independently reproduced (not just trusted the developer/orchestrator report): wrote a throwaway `apps/web/src/app/__qaTemp_startNewProblemGuard.test.tsx` (full `RouterProvider`+real `routeConfig`, captured `router.state.location.pathname`) covering BOTH the WORK-4/giveUp RESULT path and the SOLVE-2/diagnose RESULT path (the latter also opens the "#개념설명" card first, to confirm the 2차 `isConceptCardVisible` state and this guard fix don't interfere) — both pass with the fix in place. Then, to rule out a vacuous/false-positive test, temporarily reverted just the one added line and re-ran: both tests failed with `Expected: "/solve/pencilcanvas", Received: "/camera"`, exactly reproducing the originally-reported bug — confirming the test is a real, working repro and the fix is what makes it pass. Restored the fix (byte-identical to pre-revert, verified via diff), deleted the throwaway test file afterward (no leftover artifacts, confirmed via `git status`). `beginReinput()` (the pre-existing "수정" flow sharing the same `isRequestingReinput` flag) is untouched and doesn't interfere — `submitProblem()`/`recognizeOnly()` both still reset the flag to `false` at the start of a new submission, same as before. Full gate re-run from repo root: typecheck/lint green, api 288/288 (unchanged), web 383/383 (unchanged — pure bug fix, no new tests, matches expectation), build green (same pre-existing >500kB chunk warning, not new).

**Documentation-staleness finding (reported, not fixed by QA)**: `docs/PROJECT_STATUS.md` §3.21 (as of the working tree at this QA run) only documents "1차 실행 완료" and ends with "다음: 2차 실행 착수" — it does NOT document that 2차 was actually implemented, that stage-qa found the HIGH `startNewProblem`/`/camera` bug during 2차's final regression pass, or that it was subsequently fixed and re-verified. The code itself is fully caught up (2차's `conceptExplanations`/`ResultCard.title`/hashtag-toggle all present and tested, HIGH fix present and tested) — only the handoff doc lags behind. Since `docs/PROJECT_STATUS.md` is this project's explicit "다음 세션 시작 시 권장 첫 행동" source of truth (CLAUDE.md §6), flag this as a MUST-FIX-BEFORE-NEXT-STAGE documentation gap for whichever agent writes the next status entry — not a functional/code defect, so QA does not edit it directly.

**"WORK-4/SOLVE-2 단일 클릭 전환 + WorkLineEditor 삭제 + 사진 결과화면 유지 + ProblemCard 조건부 렌더" — STAGE PASS, verified 2026-09-06 (later same day, §3.21 이후 아직 PROJECT_STATUS.md에 미기록).** Scope isolated by mtime (`find apps packages docs -newermt "2026-09-06 13:00:00"`, excluding the 13:04-14:08 cluster which is 2차's own doc/code — that cluster's timestamps are all *older* than this stage's 18:xx cluster): `SolvePencilcanvasPage.tsx` (`handleDiagnose` now does `recognizeWork→diagnose` inline in one handler, no intermediate confirmation screen), `SolveLandscapePage.tsx` (+`handleEditWork` = `resetRecognizeWork()+resetDiagnose()+navigate("/solve/pencilcanvas")`; `ProblemCard` now `problemCardData !== null ? <ProblemCard/> : null` instead of always-rendered), `WorkLineList.tsx`(+`onEdit?` prop → header-only "수정" `<button>`, styled with `TEXT_LINK_STYLE`), `ProblemInputProvider.tsx` (`submitProblem`/`giveUp`/`diagnose` all gate their existing `clearCapturedImage()` call behind `if (lastInputType !== "photo")`), `problemInputFlow.test.tsx`/`cameraFlow.test.tsx` (rewritten assertions, including one flipped assertion `not.toBeInTheDocument()`→`toBeInTheDocument()` for photo persistence — confirmed the flip is justified by the new intended behavior, not a weakened test), `textLinkStyle.ts` (+`focus-visible:ring-2` a11y fix, orchestrator self-fix from design-agent audit), full deletion of `features/work-input/{WorkLineEditor.tsx,.test.tsx,elevatedCardStyle.ts}` confirmed via `grep -rn "work-input\|WorkLineEditor"` returning zero hits anywhere in `apps/web/src`+`apps/api/src`.

**Important scoping catch**: `git show HEAD:<SolvePencilcanvasPage.tsx|SolveLandscapePage.tsx> | grep WorkLineEditor` returned nothing — `WorkLineEditor` was **never actually mounted** in either live page, even before today's deletion (it was step-3 mock-only, per earlier work-order entries above). So today's deletion is dead-code cleanup, not a regression, and the "중간 재확인 단계 제거" in the task description refers to a *conceptual* two-click recognizeWork-then-diagnose flow that a NEW test (`recognizeDrawWorkThenDiagnose`, confirmed via diff as a `+`-only addition) proves is now genuinely one click — not to removing a previously-live WorkLineEditor screen.

**Pre-existing PRD gap surfaced (not caused by this stage, still open)**: PRD `WORK-3` (P0) requires "오인식 줄을 개별 수정" with AC "각 줄 옆에 수정 UI가 존재한다" — current `WorkLineList` has exactly one header-level "수정" link (routes back to the whole WORK canvas), never a per-line edit control. `docs/COMPONENT_MAP.md` §3 already documents this as an open backlog item ("WORK 단계 줄 단위 인식/수정/저신뢹도 경고 UI" — Figma frame never found) predating this stage, but that same doc's line 59 still says `WorkLineEditor`(3단계) "착수 전" (not yet started) — now stale since it was implemented as mock-only *and then deleted*. Flag for whoever next touches `COMPONENT_MAP.md`/`FRONTEND_IMPLEMENTATION_PLAN.md` §7 to update that line and for the owner to confirm whether the single header "수정"(전체 재입력) UX is the accepted final replacement for WORK-3's per-line edit AC (in which case PRD/AC wording should be updated) or still pending a proper per-line editor.

**Verification technique — extended the throwaway-test pattern to cover 3 scenarios in one temp file** (`apps/web/src/app/__qaTemp_workEditFlow.test.tsx`, written, run via `npx vitest run --reporter=verbose <path>` from `apps/web/`, then `rm`'d immediately, confirmed via `git status` clean afterward): (1) photo input → draw WORK stroke → "봐 주세요" (diagnose path, not giveUp) → `ProblemCard`/photo still visible in result — the existing product test suite only covered photo-persistence through the WORK-4/`giveUp()` path, not the SOLVE-2/`diagnose()` path, so this closed a real coverage gap; (2) handwriting-only input through the same diagnose path → zero `ProblemCard`-related alt text anywhere; (3) full-router "수정" click → `router.state.location.pathname` ends at `/solve/pencilcanvas` (not `/camera` — the exact HIGH-bug shape from the earlier `startNewProblem()` finding), WORK canvas stroke fill-call-count resumes after the click (strokes preserved), and a second "봐 주세요" click succeeds end-to-end back to `/solve/landscape`. All 3 passed on first correct attempt (one iteration needed: jsdom has no `scrollIntoView`, needed `Element.prototype.scrollIntoView = vi.fn()` exactly like `problemInputFlow.test.tsx` already does — a test-harness gap, not a product bug). Root-cause note for why this particular "수정" click is safe from the earlier guard race: `resetRecognizeWork()`/`resetDiagnose()` only reset local state inside the `useRecognizeWork`/`useDiagnose` hooks (status/data/error) and never touch `problemId`/`hasProblemInput`/`recognizeStatus` — the exact fields `RequireProblemInputGuard.isAllowed` checks — so there's no analogous race to `startNewProblem()`'s (which did clear `problemId` and needed the `isRequestingReinput` bypass flag). Confirmed this by reading the guard + both reset functions before writing the repro, then confirmed empirically.

Gate re-run independently (not trusting orchestrator's self-report): typecheck/lint green (both workspaces), api 288/288 test, web 377/377 test (orchestrator's claimed count matched), build green (same pre-existing >500kB chunk warning, not new).

**RESUME (work-order step 5) root-cause investigation, 2026-09-06 — NOT a bug, genuinely 0% implemented at every layer.** Owner reported (3rd time same day per `docs/PROJECT_STATUS.md` §3.22 line ~338, which already documented "확인, 이번 단계에서는 구현하지 않음(코드 변경 없음)") that "내 방법으로 계속"/"다른 방법으로" buttons don't appear after DIAG. Verified fresh (not from memory) across every layer: (1) frontend — `SolveLandscapePage.tsx`'s `isDiagnosisReady` branch (lines ~293-376) has an explicit comment "`ResumeModeBar`/`ResumeResultCard`는 이번 단계에서 마운트하지 않는다" and genuinely never imports/renders them; (2) types — `shared-types.Diagnosis` (8 fields: lastValidLine/stallLine/errorTypeLabel/errorDetail/relatedConcepts/reachedAnswerWithNotes/isLowConfidence/conceptExplanations) has **no DIAG-7 identified-method field**, so there's no data to even start a resume from; (3) backend — `grep -rn "resume\|Resume\|listMethods\|ResumeRequest"` across `apps/api/src`+`packages/shared-types/src`+`packages/validation/src` returns **zero hits**; `LLMAdapter` interface (`adapter.ts`) has 6 methods, missing PRD PROV-1's required `resume`/`listMethods`; only module dir is `modules/diagnosis/` (single `diagnose` endpoint), no `modules/resume/`; (4) DB — grepped all 4 files in `supabase/migrations/`, PRD §7's `method_catalog`/`resume_solutions` tables don't exist anywhere (and note `diagnoses` itself isn't in Supabase either — everything is still `inMemoryProblemStore.ts`, matching PROJECT_STATUS.md §5 "7. Supabase 저장 ❌ 미착수"); (5) prompts — no `buildResumePrompt` in `prompts/system.ts`. The only "resume"-named code that exists is `ProblemInputProvider.resumeFromHistory` — completely unrelated (MyPage "다시 풀기" = resuming a *problem session*, not RESUME-1~5 student-method continuation).

**Why:** PRD SOLVE-3(b) lists "이어풀기 또는 다해법 풀이" as a P0 result-screen component right next to the DIAG summary, which reads to a non-technical owner like "should appear automatically once diagnosis appears" — but `FRONTEND_IMPLEMENTATION_PLAN.md` §1.3.1 splits this into work-order step 5 (separate from step 4's WORK/DIAG wiring), and that split evidently hasn't landed clearly with the owner despite being logged twice already (§3.20 backlog line, §3.21 "다음", §3.22 explicit request-and-decline). **How to apply next time**: when a stage's scope explicitly excludes a PRD P0 sub-bullet that sits directly adjacent (same AC list item) to what WAS just implemented, flag in the QA/report output that the owner may reasonably expect it's already done — don't just log "out of scope" in a docs file the owner may not re-read; say it plainly in the direct response.

**Concrete finding for whoever implements RESUME**: `ResumeModeBar.tsx`/`ResumeResultCard.tsx` (work-order step 2 mock components) already exist with reasonable props (`ResumeMode`, `ResumeSolution` — currently locally-declared, will need the same shared-types promotion + dedupe treatment `Diagnosis` got between 4a-1→4b), but `Diagnosis` needs a DIAG-7 identified-method field added FIRST (diagnose() doesn't currently identify the student's method at all) before resume() has anything to act on. RESUME-5 (CAS re-verification of the resume answer) will need the same `stubCasVerification`-style deterministic stub pattern used for DIAG's CAS step, since no real CAS/SymPy service exists yet — flag this stub limitation explicitly to the owner (P0 AC literally requires failed-verification results to be hidden, and a stub that's always `verified:true` can't actually fail).

**RESUME work-order step 5 (backend 1차 + screen-wiring 2차) — STAGE CONDITIONAL PASS, verified 2026-09-07.** Backend: `LLMAdapter.resume()` (SSE), `POST /api/problems/:problemId/resume` (`resume.router.ts`, same ownership/404-if-no-diagnosis pattern as `diagnosis.router.ts`), `Diagnosis` +`identifiedMethod`/`isMethodApplicable`/`methodApplicabilityNote`, new `ResumeMode`/`ResumeRequest`/`ResumeSolution`/`ResumeStreamEvent` in shared-types (properly deduped — no local re-declarations found via `grep -rn "interface WorkLine\b\|interface Diagnosis\b\|interface CasStepVerification\b"`, the WorkLineList.tsx local `WorkLine` remains the one deliberately-distinct exception from earlier stages), `stubResumeCasCheck` (always `{verified:true}`, same owner-approved interim pattern as `stubCasVerification`). Frontend: `ResumeModeBar`/`ResumeResultCard` rewritten to Figma measurement (`255:87`/`255:92`), `AnswerBox` `tone="resume"` variant, mounted in `SolveLandscapePage.tsx`'s `isDiagnosisReady` branch, manual-trigger only (no auto-call after diagnose, owner-confirmed). `ResumeSolution.methodName` field-name-kept-but-meaning-redefined ("이어가는 지점 요약" not "해법명") is consistently applied everywhere (`fake-adapter.ts`/`prompts/system.ts`/`ResumeResultCard.tsx` all updated in sync) — no confusion found between this and `Diagnosis.identifiedMethod.methodName` (the real method name), which is used only internally in `buildResumeUserMessage`/diagnose-prompt context, never rendered directly to the student.

**Live OpenAI smoke test (own + alternative modes) independently reproduced** using the same `.mts`+`tsx --env-file` technique as the 4a-2 stage (see entry above) — confirmed real-model RESUME-1 (continues from `lastValidLine+1` with the *same* identified method), RESUME-2 (does not re-explain the already-correct lines), and RESUME-3 (each step states both action and reasoning) all hold against a live GPT response, not just mocked/fake-adapter tests.

**Two HIGH findings, both confirmed by direct code+live-adapter inspection (not present in the developer's/orchestrator's self-report)**:
1. **RESUME-4's "그 사실과 이유를 먼저 설명" AC is not met** — `Diagnosis.methodApplicabilityNote` (the LLM-generated reason a method doesn't apply) is computed by `diagnose()` but is **never read anywhere in `apps/web/src`** (`grep -rn "methodApplicabilityNote" apps/web/src` outside tests returns zero hits). `ResumeModeBar.tsx`'s `ownModeDisabled` prop is a bare boolean with no note-text prop at all — the UI only ever shows a hardcoded generic string ("이 방법으로는 이어갈 수 없어요"), so the specific reason a student's method fails is silently discarded end-to-end.
2. **RESUME-5's "실패 시 미표시, 재생성/오류 안내로 대체" AC has zero enforcement in the frontend**, independent of the already-known CAS-stub caveat. `SolveLandscapePage.tsx` renders `<ResumeResultCard>` whenever `resumeStatus==="success" && resumeSolution` — it never branches on `resumeSolution.verified`. Even after a real CAS service replaces the stub and starts returning `verified:false` sometimes, today's code would still display the unverified result exactly as if it passed. This is a distinct, separately-fixable gap from "CAS is a stub" (that part was already flagged/owner-approved in the RESUME investigation entry above; this part was not).

**Two MEDIUM findings**: (a) RESUME-4's AC literally says "대안 해법 목록(METHOD 흐름)으로 **자동 전환**하는 UI" — current implementation only disables the "own" button and requires the student to manually click "다른 방법으로"; no auto-switch exists. This may be an accepted Figma-driven UX simplification (same pattern as the earlier WORK-3 "header-level 수정 link vs per-line edit" precedent) but wasn't explicitly confirmed against this AC wording in `docs/PROJECT_STATUS.md` §3.23 — flag for owner confirmation, don't assume. (b) No backend enforcement of RESUME-4 — `resume.router.ts` never checks `diagnosis.isMethodApplicable` before running `mode:"own"`; confirmed via live smoke test that `adapter.resume()` happily generates an "own"-mode continuation even when fed a `diagnosis` with `isMethodApplicable:false`. The only gate is the disabled HTML `<button>` on the client (`Button.tsx` correctly forwards the native `disabled` attribute, so this isn't exploitable through the normal UI — but per this role's "frontend hiding is never a security control" principle, this is worth closing at the API layer too, especially before RESUME-4's UX changes in the future).

**One LOW / scope-as-planned note**: "다른 방법으로" produces exactly one alternative solution per click (single-shot `resume()`), not a "목록" of candidate methods to choose from — the fuller METHOD multi-method-list flow is work-order step 8 (not started), so this is in-scope-as-planned, not a stage defect.

**Regression re-confirmed**: `resetResume()` is correctly wired into `beginReinput()`/`startNewProblem()`/`SolveLandscapePage.handleEditWork()`; none of these three create a new guard race — `beginReinput`'s `resetRecognize()` (which does clear `problemId`) and its own `setIsRequestingReinput(true)` are both synchronous in the same callback so React 18 batches them into one commit (no race), `startNewProblem()` still sets the bypass flag as the very first line (the pre-existing HIGH-bug fix, untouched), and `handleEditWork`'s `resetRecognizeWork()`/`resetDiagnose()`/`resetResume()` never touch `problemId`/`recognizeStatus`/`hasProblemInput` at all so there's no analogous race to guard against. Verified "다른 방법으로" full click-through and resume-failure-modal-then-retry via a throwaway `apps/web/src/app/__qaTemp_resumeFlow.test.tsx` (full `RouterProvider`+real guard, written/run/deleted per the established throwaway-test technique) — the product test suite (`problemInputFlow.test.tsx`) only covers "내 방법으로 계속" success and the `isMethodApplicable:false` disabled-state, not alternative-mode or the error-modal-retry path, so this closed a real E2E coverage gap (both passed on the real code, confirming those paths work despite not being in the committed suite).

**Doc lag (not a code defect, recurring pattern already known)**: `docs/PROJECT_STATUS.md` §5 summary table row "6.6" and `docs/FRONTEND_IMPLEMENTATION_PLAN.md` §1.3.1 step-5 bullet both still describe RESUME as not-yet-connected/future-tense, even though §3.23 in the *same* `PROJECT_STATUS.md` document already logs it as done. `docs/COMPONENT_MAP.md` rows for `ResumeModeBar`/`ResumeResultCard`, by contrast, are already correctly updated — the lag is isolated to those two specific locations.

**Gate**: typecheck/lint green (both workspaces), api 302/302, web 389/389, build green (same pre-existing >500kB chunk warning, not new) — independently re-run, not trusted from the orchestrator's report.

**RESUME work-order step 5 — fix-of-the-2-HIGH-findings, re-verified STAGE PASS, 2026-09-07.** Scope isolated via `find apps packages docs -type f -newermt "2026-09-07 00:00:00"` (not mtime-vs-earlier-cluster this time — the whole rest of the tree predates today): exactly 7 files — `ResumeModeBar.tsx`(+`applicabilityNote?: string|null` prop, falls back to the old hardcoded string only when null/undefined)+test, `SolveLandscapePage.tsx`(passes `diagnosis.methodApplicabilityNote`; `<ResumeResultCard>` now gated on `resumeStatus==="success" && resumeSolution && resumeSolution.verified`; new `!resumeSolution.verified` branch renders a `Modal` with `onAction={resetResume}`), `resume.router.ts`'s `resolveResumeContext` (now takes `mode`, throws 400 `validation_error` when `mode==="own" && !stored.diagnosis.isMethodApplicable`)+test, `problemInputFlow.test.tsx`(RESUME-4 test strengthened to assert the *actual* note text and assert the old hardcoded fallback string is absent; new RESUME-5 verified:false→modal→retry-clears-modal test), `docs/PRD_WHYMATH.md`(RESUME-4 AC re-worded from "자동 전환하는 UI가 노출된다" to "...학생이 '다른 방법으로'를 선택하면 대안 해법으로 전환된다" — matches manual-click implementation, confirmed via `git diff HEAD -- docs/PRD_WHYMATH.md`, ID/P0 unchanged, this was an explicit owner decision to close a previously-flagged MEDIUM doc/impl mismatch, not a QA-initiated change). Verified `useResumeStream.ts`'s `reset()` clears `result`/`errorMessage`/`status` all the way back to `"idle"` (not just status) — this is what makes the verified:false retry path actually work (stale unverified result doesn't linger after dismissing the modal).

Both previously-reported HIGH findings independently re-confirmed closed by direct code reading (not just trusting the self-report): (1) `applicabilityNote` is real LLM text end-to-end, `grep -rn methodApplicabilityNote apps/web/src` now has non-test hits; (2) `!resumeSolution.verified` now has its own render branch, `<ResumeResultCard>` can no longer render an unverified result. The MEDIUM "no backend enforcement of RESUME-4" finding is also independently closed (`resolveResumeContext` 400s regardless of client-side disabled-button bypass) — confirmed via 2 new `resume.router.test.ts` cases (`mode:"own"`+inapplicable→400, `mode:"alternative"`+inapplicable→200, i.e. the block is `own`-specific not a blanket reject). The other MEDIUM ("no auto-switch to alternative") remains open-as-designed per the PRD wording change above — this is the owner's explicit call, not a gap to keep flagging.

Regression: confirmed via the same `-newermt` mtime scoping that RESUME-1/2/3's prompt/adapter files (`prompts/system.ts`, `openai-adapter.ts`, `fake-adapter.ts`), the `startNewProblem()` guard-race fix, and the `methodName`-dual-meaning code were **not touched at all** by this fix round — nothing to re-verify beyond confirming they weren't in today's diff. Gate independently re-run (not trusted from orchestrator report): typecheck/lint green, api 304/304 (302 baseline + 2 new `resume.router.test.ts` cases), web 392/392 (389 baseline + 3 new: 2 in `ResumeModeBar.test.tsx` for the applicabilityNote passthrough/fallback + 1 new RESUME-5 verified:false test in `problemInputFlow.test.tsx`), build green (same pre-existing >500kB chunk warning).

**Recurring doc-lag pattern reconfirmed**: `docs/PROJECT_STATUS.md` §3.23 and §5's "6.6" summary row still don't mention either the 2 HIGH findings or today's fixes for them — same lag pattern flagged in the 4b-정정 entries above. Safe for the orchestrator to update once this PASS lands, since the code+tests are now actually caught up.
