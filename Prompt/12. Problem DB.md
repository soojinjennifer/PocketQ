Stage 1

Reference + Curriculum Infrastructure

DB 구조와 출처관리부터 구현.

Stage 2

PDF Analyzer

수악중독 대수·미적분Ⅰ PDF에서 Concept/Skill/유형을 구조화.

Stage 3

Problem Family Builder

공식 교육과정 + 2028 수능 + 수악중독을 교차분석해 Family 확정.

Stage 4

Problem Generator

Family별 D2/D3/D4 문제 생성.

Stage 5

QA Pipeline

Solver / Curriculum / Difficulty / CSAT Style / Similarity 검증.

Stage 6

Supabase Production Import

통과한 자체 생성문제만 운영 DB에 등록.


plan-agent
   ↓
구현 계획 수립
   ↓
development-agent
   ↓
구현 + 자체 테스트
   ↓
stage-qa-agent
   ↓
독립 검증
   │
   ├─ PASS → 다음 Stage
   │
   └─ FAIL/Conditional
          ↓
      development-agent 수정
          ↓
      stage-qa-agent 재검증


# PocketQ Problem DB — Stage 1 Agent-Orchestrated Implementation & Stage QA

You are the orchestration agent for the PocketQ project.

This task must use the existing specialized agents:

1. `plan-agent`
2. `development-agent`
3. `stage-qa-agent`

Do not bypass these agents and implement the entire task directly.

The objective of Stage 1 is to build the data foundation for a high-quality Korean high-school mathematics problem database covering:

* 대수 (Algebra)
* 미적분Ⅰ (Calculus I)

The future PocketQ problem system must support:

* 2022 revised Korean national curriculum alignment
* 2028 CSAT scope and style
* reference-source traceability
* Problem Family based generation
* controlled difficulty
* original problem generation
* later similarity and quality QA

At Stage 1:

**DO NOT generate production mathematics problems.**

Build only:

`Reference Sources → Curriculum Map`

---

# 0. Source of Truth

Before any work, inspect:

1. `docs/PRD_WHYMATH.md`
2. `PLANNING.md`
3. latest status/progress documents
4. `CLAUDE.md`
5. existing Supabase migrations
6. existing database/schema related code
7. relevant project rules
8. existing agent definitions

The service name is:

`PocketQ`

Existing paths such as `docs/PRD_WHYMATH.md` must not be renamed unless a separate approved migration requires it.

Do not alter working PocketQ functionality unrelated to this stage.

---

# 1. Reference Source

Register this page as PocketQ's first mathematics reference source:

https://mathjk.tistory.com/3584

It contains materials organized around the 2022 revised curriculum.

## Algebra

* 지수함수와 로그함수
* 삼각함수
* 수열

## Calculus I

* 함수의 극한과 연속
* 미분
* 적분

The page appears to display or link to a CC BY 4.0 license.

However:

Do NOT automatically assume every individual problem or embedded third-party asset inside every PDF can be reused in production.

Until rights are explicitly verified, classify all MathJK material as:

`REFERENCE_ONLY`

It may later be used for analysis of:

* curriculum coverage
* concepts
* skills
* problem-family taxonomy
* reasoning patterns
* representative difficulty patterns

Do NOT:

* copy MathJK problem text into PocketQ production problem tables
* create number-only variants
* create symbol-only variants
* reproduce diagrams without verified rights
* expose reference PDFs directly to PocketQ users
* fine-tune an AI model on these PDFs during Stage 1

---

# PHASE A — PLAN AGENT

Invoke:

`plan-agent`

The plan-agent must NOT modify code.

For this stage, explicitly allow the plan-agent to analyze backend/data architecture even if its existing description contains frontend-oriented wording.

It must inspect:

* existing Supabase migrations
* current persistence architecture
* existing tables
* database access patterns
* Supabase client/server usage
* TypeScript types
* validation schemas
* scripts
* test infrastructure
* package scripts
* current git status when relevant

The plan-agent must produce a Stage 1 plan covering:

1. relevant existing architecture
2. tables to add or extend
3. migration strategy
4. files to add/change
5. source-of-truth data strategy
6. reference-source rights tracking
7. curriculum hierarchy model
8. prerequisite model
9. import/upsert strategy
10. validation strategy
11. test strategy
12. regression risks
13. Definition of Done

Prefer the smallest safe extension of the current PocketQ architecture.

Do NOT propose unrelated refactoring.

---

# 2. Required Stage 1 Data Architecture

Unless existing architecture provides a clearly better compatible equivalent, support the following.

## A. reference_sources

Create or extend:

`reference_sources`

Minimum fields:

* `id`
* `name`
* `publisher`
* `source_url`
* `source_type`
* `subject`
* `curriculum_version`
* `license_type`
* `license_url`
* `license_verified`
* `usage_mode`
* `attribution_text`
* `retrieved_at`
* `notes`
* `created_at`
* `updated_at`

`usage_mode` must support at least:

* `REFERENCE_ONLY`
* `DERIVATIVE_ALLOWED`
* `DISPLAY_ALLOWED`
* `PRODUCTION_ALLOWED`

Safety invariant:

An unverified source must never automatically become:

`PRODUCTION_ALLOWED`

---

## B. curriculum_nodes

Support the hierarchy:

`SUBJECT → UNIT → SUBUNIT → CONCEPT → SKILL`

Create or extend:

`curriculum_nodes`

Minimum fields:

* `id`
* `code`
* `subject`
* `node_type`
* `parent_id`
* `name`
* `description`
* `curriculum_version`
* `csat_importance`
* `difficulty_base`
* `allowed_scope`
* `forbidden_scope`
* `source_id`
* `is_active`
* `created_at`
* `updated_at`

Stable codes must be used.

Recommended prefixes:

* `ALG_*`
* `CALC1_*`

---

## C. curriculum_prerequisites

Create or extend:

`curriculum_prerequisites`

Minimum fields:

* `node_id`
* `prerequisite_node_id`
* `strength`
* `notes`

Prevent:

* self-reference
* duplicate relationships
* invalid node references

Provide validation for circular prerequisite chains.

---

# 3. Curriculum Source of Truth

Curriculum content must NOT exist only inside SQL migrations.

Create version-controlled data such as:

`data/math-curriculum/algebra.v1.json`

`data/math-curriculum/calculus1.v1.json`

Initial major structure:

## Algebra

1. 지수함수와 로그함수
2. 삼각함수
3. 수열

## Calculus I

1. 함수의 극한과 연속
2. 미분
3. 적분

At Stage 1:

Do NOT create the final detailed CSAT Problem Family taxonomy.

Do NOT mass-generate Skills merely to reach a target number.

Only create curriculum nodes supported by the intended curriculum structure.

Every node must have a stable unique code.

---

# 4. Reference Manifest

Create:

`data/references/mathjk/README.md`

Record:

* source URL
* retrieval date
* author/publisher if identifiable
* curriculum version
* subject coverage
* observed page-level license
* license verification status
* current allowed use
* prohibited production use
* third-party-rights uncertainty

Recommended folders:

`data/references/mathjk/algebra/`

`data/references/mathjk/calculus1/`

If reference PDFs are stored locally and redistribution rights are uncertain:

* do not commit them
* add suitable `.gitignore` protection

If PDFs are not locally available, create a manifest rather than failing the stage.

---

# 5. Import / Upsert Pipeline

Design and implement an idempotent curriculum import pipeline.

It must:

1. validate source JSON
2. upsert reference sources
3. upsert curriculum nodes
4. resolve parent relationships
5. resolve prerequisite relationships
6. avoid duplicates when rerun
7. never automatically delete production data
8. never automatically upgrade reference usage rights

Prefer stable curriculum `code` values as natural import identifiers where appropriate.

---

# PLAN REVIEW GATE

After `plan-agent` finishes, the orchestration agent must review its plan.

Reject or remove any proposal involving:

* destructive production schema changes
* deletion of production data
* unrelated refactoring
* automatic remote Supabase deployment
* copying reference problems into production
* premature problem generation
* premature embeddings
* premature fine-tuning

If no genuine BLOCKER exists, approve the remaining Stage 1 plan and continue.

Do not request user confirmation for ordinary non-destructive implementation decisions.

---

# PHASE B — DEVELOPMENT AGENT

Invoke:

`development-agent`

Provide it with the approved Stage 1 plan.

For this stage, database/backend work described in the approved plan is explicitly authorized.

The development-agent must implement only the approved Stage 1 scope.

Expected work may include:

* Supabase migrations
* TypeScript data types
* curriculum JSON
* reference manifest
* import/upsert scripts
* validation scripts
* automated tests
* required indexes and constraints

Do NOT implement:

* Problem Family generation
* production problem generation
* AI fine-tuning
* reference-problem ingestion into production
* embeddings
* vector search
* recommendation algorithms
* unrelated frontend work

Do not push changes to remote production Supabase automatically.

---

# 6. Development Validation

The development-agent must create or run checks for at least:

* duplicate curriculum codes
* missing parents
* orphan nodes
* invalid hierarchy
* invalid subject mapping
* duplicate prerequisites
* self prerequisites
* circular prerequisites
* invalid CSAT importance values
* invalid base difficulty values
* missing source information
* missing curriculum version
* unsafe reference usage_mode
* unverified source marked production-safe

The validation report should include:

## Algebra

* Units
* Subunits
* Concepts
* Skills

## Calculus I

* Units
* Subunits
* Concepts
* Skills

## Reference Sources

* Total
* Verified
* Reference Only
* Production Allowed

## Validation

* Errors
* Warnings
* PASS / FAIL

---

# 7. Local Verification

Use the existing PocketQ Supabase development workflow.

Where supported, verify:

* clean migration succeeds
* reset/rebuild succeeds
* import can run repeatedly
* rerunning import creates no duplicates
* existing migrations still work
* existing PocketQ persistence remains intact

Inspect package scripts before running commands.

Run all applicable:

* typecheck
* lint
* tests
* integration tests
* build

Do not:

* delete tests
* skip failing tests
* weaken assertions
* modify snapshots merely to obtain PASS

---

# PHASE C — STAGE QA AGENT

After development reports completion, invoke:

`stage-qa-agent`

The Stage QA Agent is the independent gatekeeper for moving from Stage 1 to Stage 2.

Do NOT use `final-qa-agent` for this normal stage-completion check.

The Stage QA Agent must independently inspect the actual codebase.

It must NOT trust:

* plan-agent reports
* development-agent completion claims
* previous PASS reports
* test names

It must follow its zero-modification rule:

`inspect → execute → verify → report`

It must NEVER fix Stage 1 defects itself.

---

# 8. Explicit Stage QA Scope

Tell `stage-qa-agent` that the completed stage is:

`PocketQ Problem DB Stage 1 — Reference & Curriculum Infrastructure`

Relevant Stage 1 verification includes:

## Reference Source Safety

Verify:

* MathJK is registered as a reference source
* license information is stored
* license verification state is explicit
* initial usage mode is safe
* unverified sources cannot become production-safe automatically
* reference problem text is not present in production problems

## Curriculum Integrity

Verify:

* Algebra hierarchy exists
* Calculus I hierarchy exists
* stable codes are unique
* hierarchy is valid
* no orphan nodes exist
* parent relationships are valid
* subject boundaries are valid
* curriculum version is recorded

## Prerequisite Integrity

Verify:

* no self-reference
* no duplicates
* no invalid references
* circular dependency validation exists and passes

## Source-of-Truth Consistency

Verify:

* curriculum data exists outside SQL
* source JSON matches imported DB representation
* imports are repeatable
* stable identifiers are maintained

## Supabase Safety

Verify:

* migration is reproducible
* import is idempotent
* existing persistence is unaffected
* no destructive production operation was introduced
* relevant RLS/security implications were considered

## Scope Protection

Verify Stage 1 did NOT prematurely implement:

* Problem Family generation
* generated production problems
* embeddings
* vector search
* fine-tuning
* similarity recommendation
* reference problem redistribution

---

# 9. Stage QA Regression

The Stage QA Agent should run focused regression tests.

Because Stage 1 primarily changes data infrastructure, test previous functionality affected by Supabase/schema changes.

At minimum inspect relevant regression risk around:

* Auth
* Grade Setup
* Solve
* Supabase Persistence
* Follow-up Chat persistence
* MyPage
* History Detail

Do not blindly rerun unrelated device/UI testing unless actual Stage 1 changes can affect those paths.

However, if the Stage QA Agent's normal rules require broader verification because changed architecture has a larger blast radius, allow it.

---

# 10. Stage QA Automated Verification

The Stage QA Agent must independently inspect actual package scripts and run applicable:

* typecheck
* lint
* test
* build

If relevant focused database/integration tests exist, run them independently.

It must not modify code, tests, migrations, config, snapshots, or documentation to make tests pass.

---

# 11. Stage QA Decision

The Stage QA Agent must return its standard PocketQ Stage QA format.

The final Stage decision must be exactly one of:

`STAGE PASS — READY FOR NEXT STAGE`

`STAGE CONDITIONAL PASS — FIX REQUIRED`

`STAGE FAIL — DO NOT PROCEED`

Do NOT reinterpret or soften the Stage QA result.

---

# PHASE D — DEFECT LOOP

If Stage QA returns:

`STAGE PASS — READY FOR NEXT STAGE`

Stage 1 is complete.

Stop and report readiness for Stage 2.

---

If Stage QA returns:

`STAGE CONDITIONAL PASS — FIX REQUIRED`

or

`STAGE FAIL — DO NOT PROCEED`

do NOT start Stage 2.

Collect the exact Stage QA defect list.

Classify defects into:

### Implementation defect

Examples:

* broken migration
* import duplicates
* invalid constraint
* missing validation
* incorrect file
* test failure

Send the exact defect list to:

`development-agent`

and request the smallest safe correction.

---

### Architecture / requirement defect

Examples:

* schema fundamentally wrong
* source-of-truth design conflict
* existing PocketQ architecture incompatible
* incorrect scope interpretation

Invoke:

`plan-agent`

first to revise only the affected part of the plan.

Then invoke:

`development-agent`

with the revised approved fix plan.

---

# 12. Mandatory Re-QA

After every defect fix:

invoke `stage-qa-agent` again.

Do NOT accept:

* development-agent saying "fixed"
* local tests passing
* orchestration agent inspection

as sufficient proof.

Only a fresh Stage QA result of:

`STAGE PASS — READY FOR NEXT STAGE`

allows Stage 2 to begin.

---

# 13. Definition of Done

Stage 1 is complete only when:

1. reference-source rights are explicitly traceable
2. Algebra curriculum hierarchy exists reproducibly
3. Calculus I curriculum hierarchy exists reproducibly
4. curriculum source data is version controlled outside SQL
5. imports are idempotent
6. migrations are reproducible
7. duplicate codes do not exist
8. orphan curriculum nodes do not exist
9. prerequisite integrity checks pass
10. unverified references remain non-production
11. MathJK problem text has not entered production problem tables
12. no premature production problem generation exists
13. applicable tests/build pass
14. existing PocketQ functionality remains intact
15. Stage QA returns:

`STAGE PASS — READY FOR NEXT STAGE`

---

# 14. Final Stage 1 Report

After Stage QA passes, return:

## PocketQ Problem DB Stage 1 Report

### Plan

* architecture selected
* key decisions

### Implementation

* files created
* files modified
* migrations created
* database tables added/extended

### Curriculum

* Algebra node counts
* Calculus I node counts

### Reference Sources

* sources registered
* license status
* usage mode

### Validation

* curriculum integrity
* prerequisite integrity
* import idempotency
* migration reproducibility

### Automated Checks

* typecheck
* lint
* tests
* build

### Stage QA

* requirement results
* regression results
* issues by severity
* NOT VERIFIED items

### Unresolved Issues

* licensing
* technical debt
* external dependencies

### Stage Decision

Output:

`PASS — READY FOR STAGE 2`

only if `stage-qa-agent` returned:

`STAGE PASS — READY FOR NEXT STAGE`

Otherwise report the corresponding incomplete status.

Stop after Stage 1.

Do NOT start Stage 2 automatically.
---------
# PocketQ Problem DB — Stage 2
# Pilot Reference Analysis & Problem Family Candidate Extraction

You are the orchestration agent for the PocketQ project.

Use the existing agents sequentially:

1. plan-agent
2. development-agent
3. stage-qa-agent

Do not bypass these agents.

Stage 1 has already passed Stage QA.

Stage 2 objective:

Analyze exactly ONE pilot reference PDF:

"수악중독 유형 - 대수 - 1. 지수함수와 로그함수.pdf"

and build a reproducible pipeline that converts reference problems into
structured mathematical FEATURES and Problem Family CANDIDATES.

DO NOT generate production PocketQ problems in this stage.

DO NOT start Stage 3 automatically.

--------------------------------------------------
SOURCE OF TRUTH
--------------------------------------------------

Read:

- docs/PRD_WHYMATH.md
- PLANNING.md
- Stage 1 implementation
- current Supabase migrations
- data/math-curriculum/algebra.v1.json
- reference_sources schema
- curriculum_nodes
- curriculum_prerequisites
- current tests/scripts
- relevant agent definitions

Service name:

PocketQ

Existing filename docs/PRD_WHYMATH.md must remain unchanged unless
a separately approved migration changes it.

--------------------------------------------------
REFERENCE PRIORITY
--------------------------------------------------

Use this hierarchy:

1. Official 2022 Revised Korean Curriculum
   -> determines what is in/out of scope.

2. MathJK pilot PDF
   -> reference for concept coverage,
      problem patterns and reasoning structures.

MathJK is still:

usage_mode = REFERENCE_ONLY

Do not upgrade its rights status automatically.

The following sources are NOT part of this Stage 2 pilot:

- ProblemDB/
- AI Hub bulk files
- other MathJK PDFs
- commercial problem books
- arbitrary web problems

The analyzer must use an explicit input allowlist.

Never recursively ingest every PDF/data file in the repository.

--------------------------------------------------
PHASE A — PLAN AGENT
--------------------------------------------------

Invoke plan-agent.

It must inspect the Stage 1 architecture and propose the smallest safe
extension for reference-feature extraction.

It must NOT modify code.

The plan must determine whether the existing architecture should add
or extend equivalents of:

reference_documents
reference_item_features
problem_family_candidates

Do NOT create final production problem_families yet unless the existing
architecture clearly requires that name.

Candidate data must remain distinguishable from approved production data.

The plan must specify:

- exact pilot PDF input path
- document identity/hash strategy
- page/item locator strategy
- transient extraction strategy
- feature schema
- curriculum mapping strategy
- candidate-family grouping strategy
- deduplication strategy
- difficulty-feature strategy
- copyright/raw-text safety strategy
- idempotency strategy
- validation
- tests
- Stage 2 Definition of Done

Do not propose unrelated refactoring.

--------------------------------------------------
PDF INPUT SAFETY
--------------------------------------------------

Expected pilot input location:

data/references/mathjk/algebra/
수악중독 유형 - 대수 - 1. 지수함수와 로그함수.pdf

The PDF must remain gitignored unless redistribution rights are
explicitly verified.

If the exact PDF does not exist locally:

- do not substitute another PDF
- do not scan ProblemDB/
- do not silently ingest web search results

Report the missing input clearly.

Prefer native PDF text extraction.

If mathematical notation cannot be reliably extracted with native
text extraction, use the smallest necessary fallback and record the
extraction limitation.

Do not treat extraction output as production content.

--------------------------------------------------
REFERENCE DOCUMENT MODEL
--------------------------------------------------

Create or extend a document-level record capable of storing:

- source_id
- document_key
- filename
- subject
- unit
- curriculum_version
- file_hash
- page_count
- extraction_version
- parser_version
- usage_mode
- license_status
- processed_at

Reprocessing the same document with the same pipeline version
must not create duplicate records.

--------------------------------------------------
REFERENCE ITEM FEATURES
--------------------------------------------------

For each identifiable reference item, extract STRUCTURED FEATURES.

Do not persist full original problem text to production-facing tables.

Store only what is necessary to understand the mathematical DNA.

Suggested feature categories:

- reference_document_id
- local_item_key
- page_number
- curriculum_node_codes
- primary_concept
- secondary_concepts
- required_skills
- prerequisite_skills
- representation_type
- answer_format
- condition_count
- reasoning_pattern
- reasoning_step_count
- calculation_load
- concept_load
- transformation_pattern
- graph_or_diagram_required
- common_trap_candidate
- approximate_difficulty
- family_signature
- extraction_confidence
- review_status

representation_type may classify forms such as:

- expression
- equation
- inequality
- graph
- function relation
- word/problem situation
- mixed

Do not force values when evidence is insufficient.

Use UNKNOWN / review-required states where appropriate.

--------------------------------------------------
RAW CONTENT RULE
--------------------------------------------------

Reference problem text, images and diagrams may exist only in
temporary/local extraction artifacts needed for analysis.

They must NOT be inserted into PocketQ production-facing problem tables.

Do not create:

- number-only variants
- symbol-only variants
- close paraphrases
- translated copies
- redrawn copies of source diagrams

Stage 2 is FEATURE EXTRACTION ONLY.

--------------------------------------------------
CURRICULUM MAPPING
--------------------------------------------------

Each extracted item must map to existing Stage 1 curriculum nodes
where evidence supports the mapping.

Classify mapping as:

EXACT
MULTI_NODE
UNCERTAIN
OUT_OF_SCOPE

Never invent a curriculum node solely to make mapping succeed.

If repeated unmapped patterns appear, report them as curriculum-gap
candidates instead of automatically modifying Stage 1 taxonomy.

Every OUT_OF_SCOPE item must be excluded from Family candidate creation.

--------------------------------------------------
PROBLEM FAMILY CANDIDATES
--------------------------------------------------

Convert item-level features into candidate families.

A Problem Family represents a shared mathematical solving structure,
NOT merely similar wording.

Candidate-family identity should consider:

- primary concept
- required skills
- reasoning sequence
- transformation pattern
- representation type
- answer structure

It must NOT be determined mainly by:

- identical nouns
- similar sentence wording
- changed numeric constants
- cosmetic symbol changes

Candidate fields should support:

- candidate_code
- subject
- unit
- curriculum_node_codes
- family_name
- core_concept
- required_skills
- reasoning_signature
- canonical_reasoning_steps
- representation_types
- prerequisite_nodes
- approximate_difficulty_min
- approximate_difficulty_max
- source_item_count
- evidence_item_keys
- confidence
- status

Candidate status must support at least:

CANDIDATE
REVIEW_REQUIRED
REJECTED

Do NOT mark candidates as final production-approved families.

--------------------------------------------------
DIFFICULTY
--------------------------------------------------

Do NOT ask an LLM merely:

"is this easy or hard?"

Approximate difficulty must be derived from observable features.

Use at least:

- number of concepts
- prerequisite depth
- reasoning steps
- calculation load
- condition interpretation
- representation conversion
- need for case distinction
- need for non-obvious transformation

Use the PocketQ D1-D5 vocabulary if already established,
but in Stage 2 the score is only:

REFERENCE_ESTIMATE

It must NOT yet be treated as calibrated CSAT difficulty.

Final CSAT calibration belongs to a later stage using official
2028 sample/KICE evidence.

--------------------------------------------------
FAMILY DEDUPLICATION
--------------------------------------------------

The pipeline must identify:

- exact duplicates
- near-duplicate candidate families
- same reasoning structure with different surface form
- surface-similar items with genuinely different reasoning

Merging must be explainable.

Do not merge solely on embedding/text similarity.

Produce a merge/evidence report.

--------------------------------------------------
STAGE 1 LOW ISSUE
--------------------------------------------------

Stage 1 reported unused helper:

isKnownReferenceSourceType()

If appropriate, connect it to Stage 2 source/import validation.

If it is genuinely unnecessary, remove only the dead helper/comment
using the smallest safe change.

Do not perform unrelated cleanup.

--------------------------------------------------
PROBLEMDB SAFETY
--------------------------------------------------

The repository contains an untracked ProblemDB/ directory containing
1000+ AI-Hub-related files.

Stage 2 must NOT ingest, scan, classify, copy or modify those files.

Ensure the pilot analyzer has an explicit source allowlist.

If necessary, add a safe ignore/exclusion rule without deleting
the directory.

--------------------------------------------------
PHASE B — DEVELOPMENT AGENT
--------------------------------------------------

After plan review, invoke development-agent with the approved plan.

Explicitly authorize:

- data/backend code
- Supabase migrations
- TypeScript types
- PDF/reference extraction scripts
- validation scripts
- tests

Do NOT authorize:

- final problem generation
- recommendation engine
- embeddings for production search
- AI fine-tuning
- UI work
- production Supabase push

Implement only the pilot pipeline.

--------------------------------------------------
VALIDATION
--------------------------------------------------

Create/run checks for:

- exact document identity
- duplicate document ingestion
- duplicate item keys
- invalid curriculum node references
- out-of-scope mappings
- missing required feature fields
- candidate family code duplicates
- inconsistent candidate signatures
- invalid candidate evidence references
- production tables containing reference raw text
- accidental ProblemDB ingestion
- unverified source upgraded to production-safe
- rerun idempotency

Produce a human-readable Stage 2 analysis report.

Report at minimum:

Pilot Document
- filename
- hash
- pages
- extraction status

Reference Items
- detected
- successfully analyzed
- uncertain
- out-of-scope
- failed extraction

Curriculum Mapping
- exact
- multi-node
- uncertain
- out-of-scope

Family Candidates
- candidate count
- review required
- rejected
- duplicate/merged groups

Difficulty Reference Estimate
- D1
- D2
- D3
- D4
- D5
- unclassified

Copyright Safety
- raw text in production tables: YES/NO
- source usage mode
- license verification status

--------------------------------------------------
PHASE C — STAGE QA
--------------------------------------------------

After development reports completion, invoke stage-qa-agent.

Completed stage:

PocketQ Problem DB Stage 2
Pilot Reference Analysis & Problem Family Candidate Extraction

Stage QA must independently verify actual code, scripts, database
changes and pilot output.

It must NOT fix defects.

In addition to its normal rules, explicitly verify:

1. only the exact pilot PDF was analyzed
2. ProblemDB/ was not ingested
3. MathJK remains REFERENCE_ONLY
4. source raw text is not in production problem tables
5. extracted features map to valid curriculum nodes
6. OUT_OF_SCOPE items are excluded
7. candidate families are based on mathematical reasoning structure
8. candidate families are not superficial number variants
9. candidate status remains non-production
10. approximate difficulty is feature-derived, not arbitrary LLM labeling
11. document/item processing is idempotent
12. existing PocketQ functionality has no regression

Stage QA must manually inspect a representative sample of extracted
records against the pilot source where technically possible.

It must report any limitation as NOT VERIFIED rather than PASS.

Run applicable:

- typecheck
- lint
- tests
- build
- focused Stage 2 tests

Final decision must be exactly:

STAGE PASS — READY FOR NEXT STAGE

STAGE CONDITIONAL PASS — FIX REQUIRED

STAGE FAIL — DO NOT PROCEED

--------------------------------------------------
DEFECT LOOP
--------------------------------------------------

If Stage QA is not PASS:

- do not start Stage 3
- send implementation defects to development-agent
- send architecture/scope defects to plan-agent first
- fix only the identified defects
- rerun stage-qa-agent

Only a fresh:

STAGE PASS — READY FOR NEXT STAGE

allows Stage 2 completion.

--------------------------------------------------
DEFINITION OF DONE
--------------------------------------------------

Stage 2 is complete when:

- one exact MathJK Algebra pilot PDF is reproducibly analyzed
- every usable reference item has structured mathematical features
- curriculum mappings are explicit
- unmapped/out-of-scope items are visible
- Problem Family candidates are generated from reasoning structure
- candidates are deduplicated
- no raw reference problem is promoted to production
- no PocketQ production problems have been generated
- MathJK remains REFERENCE_ONLY
- ProblemDB/ remains untouched
- applicable automated checks pass
- Stage QA returns PASS

Stop after Stage 2.

Do NOT begin CSAT/KICE calibration or problem generation.
-----------

# PocketQ Problem DB — Stage 3

# CSAT Gold-Set Calibration & Problem Family Approval

You are the orchestration agent for the PocketQ project.

Use the existing specialized agents sequentially:

1. `plan-agent`
2. `development-agent`
3. `stage-qa-agent`

Do not bypass these agents.

Stage 1 and Stage 2 have already been completed.

Proceed only if the latest Stage 2 QA decision is:

`STAGE PASS — READY FOR NEXT STAGE`

If Stage 2 is not PASS, stop.

---

# STAGE 3 OBJECTIVE

Stage 2 produced Problem Family CANDIDATES from the pilot MathJK reference material.

Stage 3 must determine:

> Which candidate families are genuinely relevant to the Korean CSAT,
> how important they are,
> where they sit in the expected difficulty spectrum,
> and which families should become approved PocketQ Problem Families.

Use official CSAT evidence as the calibration authority.

Stage 3 pipeline:

`Stage 2 Family Candidates`
→ `Official 2028 CSAT Gold Set`
→ `Compatible Historical KICE Evidence`
→ `Curriculum Compatibility Check`
→ `CSAT Relevance Scoring`
→ `Difficulty Calibration`
→ `Coverage Analysis`
→ `Approved Problem Families`

DO NOT generate production PocketQ problems yet.

Problem generation begins only in a later stage.

---

# 0. SOURCE OF TRUTH

Read:

* `docs/PRD_WHYMATH.md`
* `PLANNING.md`
* latest Stage 1 report
* latest Stage 2 report
* Stage 2 Stage-QA result
* existing Supabase migrations
* `curriculum_nodes`
* `curriculum_prerequisites`
* Stage 2 reference document/item feature structures
* Stage 2 Problem Family candidates
* related scripts/tests
* agent definitions

Service name:

`PocketQ`

Do not rename existing paths such as:

`docs/PRD_WHYMATH.md`

unless explicitly approved elsewhere.

---

# 1. EVIDENCE PRIORITY

Use the following hierarchy.

## GOLD

Official:

`2028학년도 대학수학능력시험 예시문항`

published by the Korean education authorities.

This is the highest-priority evidence for:

* question style
* scope
* reasoning structure
* expected integration of concepts
* difficulty positioning
* CSAT relevance

If Gold evidence conflicts with MathJK pattern frequency:

**Gold wins.**

---

## SILVER A

Historical KICE mathematics questions that can be reliably mapped to the 2022 revised:

* 대수
* 미적분Ⅰ

content.

Prioritize official KICE material such as:

* CSAT
* June Mock Assessment
* August/September Mock Assessment as applicable

Use historical questions only after curriculum compatibility evaluation.

Older questions must NOT automatically be treated as directly representative of the 2028 system.

---

## SILVER B

MathJK Stage 2 reference features.

Use them for:

* type coverage
* pattern diversity
* reasoning-pattern support

Do NOT use MathJK frequency alone to determine CSAT importance.

MathJK remains:

`REFERENCE_ONLY`

unless its rights status has independently changed through explicit verification.

---

# 2. COPYRIGHT / SOURCE SAFETY

Maintain source-specific rights.

MathJK:

`REFERENCE_ONLY`

Do NOT send MathJK raw PDF pages/problem text to external AI APIs if the current project policy prohibits it.

Do NOT copy MathJK raw question text into production tables.

Historical KICE material must also be treated as reference material unless its exact usage rights are verified.

For calibration, prefer storing:

* source identity
* year
* exam type
* question number
* curriculum mapping
* extracted mathematical features
* reasoning structure
* difficulty evidence

rather than full original question text.

Official source licensing metadata must be recorded where available.

Do not mix licensing status between sources.

---

# PHASE A — PLAN AGENT

Invoke:

`plan-agent`

The plan-agent must NOT modify code.

Ask it to inspect the existing Stage 1–2 architecture and design the smallest compatible Stage 3 extension.

The plan must cover:

1. official Gold-Set ingestion strategy
2. historical KICE reference strategy
3. curriculum compatibility rules
4. calibration data schema
5. Family-to-evidence relationship
6. CSAT relevance scoring
7. difficulty calibration
8. evidence confidence
9. Family approval/rejection workflow
10. coverage metrics
11. validation strategy
12. tests
13. regression impact
14. Stage 3 Definition of Done

Do not propose unrelated refactoring.

---

# 3. CALIBRATION DATA MODEL

Extend the existing architecture rather than duplicating Stage 2 structures.

The model should support equivalents of:

## `exam_reference_sets`

Possible fields:

* `id`
* `source_id`
* `exam_code`
* `exam_type`
* `exam_year`
* `curriculum_version`
* `authority`
* `evidence_tier`
* `license_status`
* `usage_mode`
* `document_key`
* `file_hash`
* `is_active`
* timestamps

Example evidence tiers:

* `GOLD_2028_SAMPLE`
* `SILVER_KICE`
* `REFERENCE_OTHER`

---

## `exam_item_features`

Store structural features, not necessarily raw copyrighted question text.

Possible fields:

* `id`
* `exam_reference_set_id`
* `item_number`
* `subject_mapping`
* `curriculum_node_codes`
* `primary_concept`
* `secondary_concepts`
* `required_skills`
* `prerequisite_skills`
* `representation_type`
* `reasoning_signature`
* `reasoning_step_count`
* `calculation_load`
* `concept_load`
* `condition_interpretation_load`
* `case_split_required`
* `representation_conversion`
* `non_obvious_transformation`
* `answer_format`
* `curriculum_compatibility`
* `extraction_confidence`
* `review_status`

---

## `problem_family_evidence`

Connect:

`Problem Family Candidate`
↔
`Official/Reference Exam Item`

Possible fields:

* `family_id`
* `exam_item_id`
* `match_type`
* `structural_similarity`
* `skill_overlap`
* `reasoning_overlap`
* `curriculum_compatibility`
* `evidence_weight`
* `notes`

Match types should support:

* `DIRECT`
* `PARTIAL`
* `COMPOSITE`
* `WEAK`
* `NONE`

Do NOT determine Family match mainly from text similarity.

---

## `problem_family_calibration`

Possible fields:

* `family_id`
* `csat_relevance_score`
* `csat_relevance_level`
* `difficulty_center`
* `difficulty_min`
* `difficulty_max`
* `gold_evidence_count`
* `silver_evidence_count`
* `coverage_confidence`
* `calibration_version`
* `calibrated_at`
* `status`

Status should support:

* `CANDIDATE`
* `APPROVED`
* `REVIEW_REQUIRED`
* `REJECTED`

---

# 4. 2028 GOLD SET

Acquire and register the official:

`2028학년도 대학수학능력시험 예시문항`

Use the official education-authority source.

Do not silently use a blog, academy reconstruction, answer site, or commercial reproduction when the official source is available.

For the mathematics section:

analyze only content relevant to PocketQ's current Stage 3 scope:

* 대수
* 미적분Ⅰ

If an item uses 확률과 통계 or another scope outside the current project:

mark it:

`OUT_OF_CURRENT_SCOPE`

Do not force it into an Algebra or Calculus I Family.

---

# 5. GOLD ITEM FEATURE EXTRACTION

For each relevant official item, extract structural mathematical features.

Do NOT simply classify by chapter.

Capture:

* core concept
* secondary concepts
* prerequisite concept
* required skill sequence
* reasoning sequence
* representation
* number of meaningful reasoning transitions
* calculation burden
* condition interpretation burden
* case distinction
* representation conversion
* hidden/implicit relation recognition
* non-obvious transformation
* final answer structure

Example conceptual representation:

Problem
→ identify function relation
→ transform condition
→ connect two concepts
→ derive constraint
→ calculate answer

The reasoning structure is more important than surface wording.

---

# 6. HISTORICAL KICE EVIDENCE

Historical KICE questions must be used only as supporting evidence.

For every historical question, assign:

`curriculum_compatibility`

exactly one of:

* `DIRECT_COMPATIBLE`
* `PARTIAL_COMPATIBLE`
* `INCOMPATIBLE`
* `UNCERTAIN`

Only:

`DIRECT_COMPATIBLE`

and carefully reviewed:

`PARTIAL_COMPATIBLE`

items may influence Stage 3 calibration.

`INCOMPATIBLE` items must not affect:

* Family approval
* CSAT relevance
* difficulty calibration

Do not use old questions merely because their chapter names look similar.

---

# 7. CSAT RELEVANCE SCORE

Do NOT let an LLM arbitrarily assign:

"this looks important: 90/100."

Calculate relevance from explicit evidence.

Design an explainable scoring model.

Suggested dimensions:

### A. Gold Evidence

Weight: highest

Does the Family appear directly or structurally in official 2028 sample questions?

### B. Curriculum Centrality

How central is the Family to official curriculum Skills and prerequisite structure?

### C. Historical KICE Evidence

Does a compatible reasoning structure recur in official historical assessments?

### D. Reasoning Reusability

Can the Family combine with other curriculum nodes in authentic CSAT-style questions?

### E. Reference Coverage

Does Stage 2 show that this is a stable recognized mathematical problem pattern?

MathJK/reference frequency must receive lower weight than official evidence.

Example model only:

Gold Evidence       40%
Curriculum          25%
Historical KICE     20%
Reasoning Utility   10%
Reference Coverage   5%

Do NOT hardcode these percentages until plan-agent evaluates whether a better evidence-based model fits the existing data.

The final score must be explainable from stored evidence.

---

# 8. RELEVANCE LEVEL

Translate the score into an interpretable level such as:

`CORE`
`HIGH`
`MEDIUM`
`LOW`
`REJECT`

Example interpretation:

CORE

* essential CSAT family
* strong curriculum centrality
* Gold and/or repeated KICE evidence

HIGH

* highly useful family
* strong official evidence

MEDIUM

* valid CSAT-compatible family
* useful for coverage and practice

LOW

* curriculum-valid but weak CSAT evidence

REJECT

* artificial, out-of-scope, duplicated, or unsupported

Do not optimize counts to fit a predetermined distribution.

---

# 9. DIFFICULTY CALIBRATION

Stage 2 difficulty was:

`REFERENCE_ESTIMATE`

Stage 3 must produce:

`CSAT_CALIBRATED`

difficulty.

Retain PocketQ:

D1
D2
D3
D4
D5

But do NOT equate difficulty simply with question number.

Difficulty must consider structural features.

Suggested dimensions:

* prerequisite depth
* concept count
* reasoning step count
* condition interpretation
* representation conversion
* case distinction
* non-obvious inference
* calculation load

Important:

High calculation burden alone must NOT automatically produce D4/D5.

The system should penalize artificial "hardness by tedious computation."

Target meanings:

### D1

Direct concept confirmation.

### D2

Standard single/few-concept application.

### D3

Typical CSAT standard reasoning.

### D4

Multi-step or integrated reasoning requiring a meaningful insight.

### D5

Upper-discrimination reasoning that remains inside curriculum and does not rely on artificial tricks.

Difficulty must store:

* structural predicted difficulty
* evidence-supported calibrated difficulty
* confidence

---

# 10. DIFFICULTY BAND, NOT SINGLE MAGIC NUMBER

A Family should normally have a difficulty range.

Example:

`ALG_LOG_GRAPH_INTERSECTION`

may support:

D2 → D4

rather than being permanently D3.

Store:

* `difficulty_min`
* `difficulty_center`
* `difficulty_max`

Future generated problems must remain within that valid Family range.

This will later prevent the generator from creating absurdly hard variants of fundamentally basic Families.

---

# 11. FAMILY APPROVAL

After calibration, evaluate each Stage 2 candidate.

Approve a Family only if:

1. curriculum-valid
2. mathematically coherent
3. distinct from other Families
4. supported by meaningful reference evidence
5. structurally useful for CSAT preparation
6. has explainable reasoning signature
7. has a valid difficulty range

A candidate may become:

`APPROVED`

`REVIEW_REQUIRED`

`REJECTED`

Do not auto-approve everything produced in Stage 2.

---

# 12. FAMILY MERGE / SPLIT REVIEW

Stage 3 official evidence may reveal Stage 2 taxonomy problems.

Detect cases where:

### Merge needed

Two Families differ only by superficial representation but use the same meaningful reasoning structure.

### Split needed

One Family contains multiple genuinely different solution strategies.

Do not automatically rewrite the taxonomy without evidence.

Produce explicit:

* merge proposals
* split proposals
* rationale
* affected candidate codes

Apply only high-confidence non-destructive changes supported by the approved plan.

---

# 13. COVERAGE METRICS

Create meaningful coverage metrics.

At minimum calculate:

## Gold Family Coverage

Percentage of relevant 2028 Gold items explainable by existing approved/candidate Families.

Target metric:

`Gold Family Coverage`

Do NOT fabricate a target PASS percentage if the Gold Set itself reveals missing taxonomy.

Report the actual value first.

---

## Gold Skill Coverage

Percentage of relevant Gold-item Skills represented by current Curriculum/Family structures.

---

## Unsupported Family Rate

Percentage of Stage 2 candidates with no meaningful official or compatible evidence.

---

## Family Gap Count

Official Gold reasoning patterns that cannot be represented by any current Family.

Each gap must be reported.

Do not silently create Families just to make coverage reach 100%.

---

# 14. HIGH-PRECISION DB PRINCIPLE

The objective is NOT:

"have the largest number of Families."

The objective is:

"cover authentic CSAT mathematical reasoning with the smallest coherent set of reusable Families."

Prefer:

high precision
+
high official coverage

over:

hundreds of artificial micro-types.

---

# PHASE B — DEVELOPMENT AGENT

After plan-agent completes and the orchestration agent reviews the plan:

invoke:

`development-agent`

Explicitly authorize:

* backend/data architecture
* Supabase migrations
* official reference ingestion
* feature extraction scripts
* calibration scripts
* scoring
* validation
* tests
* reports

Do NOT authorize:

* production problem generation
* student recommendation
* embeddings for live search
* AI fine-tuning
* unrelated frontend changes

Do NOT automatically push to production Supabase.

---

# 15. VALIDATION

Implement/run validation for:

* duplicate exam-set identity
* duplicate item identity
* invalid curriculum nodes
* incompatible historical items affecting calibration
* evidence pointing to missing Family
* duplicate Family calibration
* unsupported score values
* invalid difficulty ranges
* approved Family without evidence
* rejected Family referenced as approved
* unexplained score calculation
* raw reference text leaking to production problems
* MathJK usage mode changed unexpectedly

---

# 16. STAGE 3 REPORT

Produce:

## Gold Set

* relevant math items analyzed
* Algebra items
* Calculus I items
* out-of-current-scope items
* uncertain items

## Historical Evidence

* total considered
* direct compatible
* partial compatible
* incompatible
* uncertain

## Family Calibration

* candidates reviewed
* approved
* review required
* rejected
* merge proposed
* split proposed

## CSAT Relevance

* CORE
* HIGH
* MEDIUM
* LOW
* REJECT

## Difficulty

* D1-valid Families
* D2-valid Families
* D3-valid Families
* D4-valid Families
* D5-valid Families

Remember that one Family may span multiple levels.

## Coverage

* Gold Family Coverage
* Gold Skill Coverage
* Family Gap Count
* Unsupported Family Rate

## Rights Safety

* raw source text in production problems
* MathJK usage mode
* official source metadata
* license unresolved items

---

# PHASE C — STAGE QA AGENT

After implementation completes:

invoke:

`stage-qa-agent`

Completed stage:

`PocketQ Problem DB Stage 3 — CSAT Gold-Set Calibration & Problem Family Approval`

The Stage QA Agent must independently verify the actual code and data.

It must NOT fix defects.

In addition to normal Stage QA rules, verify:

1. official 2028 source is actually used as Gold evidence
2. MathJK does not outrank Gold evidence
3. historical KICE items undergo curriculum compatibility classification
4. incompatible old questions do not affect scores
5. CSAT relevance score is explainable
6. difficulty is feature-derived
7. calculation burden alone does not create artificial D4/D5
8. Family difficulty ranges are valid
9. approved Families have actual evidence
10. Gold Family Coverage is calculated from real mappings
11. missing Gold patterns are reported rather than hidden
12. candidate taxonomy was not inflated merely to reach high coverage
13. reference raw text did not enter production problem tables
14. no production problems were generated
15. existing PocketQ functionality has no regression

Stage QA must manually inspect representative examples from:

* CORE Family
* MEDIUM Family
* rejected Family
* D2 Family
* D3 Family
* D4/D5 Family if present
* merge/split proposal
* Gold-gap item if present

If inspection cannot actually be performed:

report:

`NOT VERIFIED`

Do not infer PASS.

---

# 17. AUTOMATED QA

Stage QA should independently run all applicable:

* typecheck
* lint
* tests
* build
* Stage 3 focused tests
* calibration validation

Do not weaken or modify tests.

Final decision must be exactly one:

`STAGE PASS — READY FOR NEXT STAGE`

`STAGE CONDITIONAL PASS — FIX REQUIRED`

`STAGE FAIL — DO NOT PROCEED`

---

# 18. DEFECT LOOP

If Stage QA is not PASS:

Do NOT start Stage 4.

For implementation defects:

invoke:

`development-agent`

with the exact defect list.

For architecture/scoring-model defects:

invoke:

`plan-agent`

first, then:

`development-agent`

After every correction:

invoke:

`stage-qa-agent`

again.

Only a fresh:

`STAGE PASS — READY FOR NEXT STAGE`

completes Stage 3.

---

# DEFINITION OF DONE

Stage 3 is complete only when:

1. official 2028 CSAT sample is registered as Gold evidence
2. relevant Algebra/Calculus I official items have structural features
3. historical evidence is curriculum-compatibility filtered
4. every approved Family has explainable evidence
5. CSAT relevance is calibrated
6. difficulty range is calibrated
7. unsupported candidates are identified
8. duplicate/incorrect taxonomy is surfaced
9. Gold coverage is measured honestly
10. Gold gaps are explicitly reported
11. no production problems have been generated
12. source/licensing boundaries remain intact
13. automated verification passes
14. Stage QA returns PASS

Stop after Stage 3.

Do NOT automatically start Stage 4.


-----
# PocketQ Problem DB — Stage 3.5

# Calibration Recovery & Evidence Reinforcement

You are the orchestration agent for the PocketQ project.

Use the existing specialized agents sequentially:

1. `plan-agent`
2. `development-agent`
3. `stage-qa-agent`

Do not bypass these agents.

Stage 1, Stage 2, and Stage 3 have already been completed.

Stage 3 result currently has:

* `APPROVED family = 0`
* the strongest current candidate is approximately:

  * `FAM-ALG-EXPLOG-009`
  * `status = REVIEW_REQUIRED`
  * `csat relevance = LOW`
  * `skillOverlap ≈ 0.333`
  * `reasoningOverlap ≈ 0.500`

Do NOT manually promote this Family to APPROVED.

Do NOT lower thresholds simply to create an APPROVED Family.

The objective of Stage 3.5 is:

> determine why Stage 3 produced zero approved Families,
> reinforce only the missing evidence/data required for valid calibration,
> rerun Stage 3 calibration,
> and obtain evidence-backed APPROVED Families if the data genuinely supports them.

---

# 0. SCOPE

Limit this recovery work initially to:

`대수 > 지수함수와 로그함수`

Do NOT immediately expand to all Algebra or Calculus I.

Do NOT mass-expand Stage 2 reference analysis.

Do NOT start Stage 4.

This is a focused diagnostic and reinforcement stage.

---

# 1. SOURCE OF TRUTH

Read before planning:

* `docs/PRD_WHYMATH.md`
* `PLANNING.md`
* Stage 1 report
* Stage 2 report
* Stage 3 report
* latest Stage 3 Stage-QA result
* current Supabase migrations
* `curriculum_nodes`
* `curriculum_prerequisites`
* Stage 2 Problem Family candidates
* Stage 3 calibration/evidence tables
* Stage 3 scoring implementation
* official 2028 CSAT Gold Set mappings
* historical KICE compatibility mappings
* existing tests/scripts
* agent definitions

Service name:

`PocketQ`

Do not rename existing file paths unless separately approved.

---

# PHASE A — PLAN AGENT

Invoke:

`plan-agent`

The plan-agent must NOT modify code.

Its first task is NOT to propose fixes.

It must first diagnose why:

`APPROVED family = 0`

for the current pilot scope.

Analyze the following possible causes independently.

---

# 2. ROOT-CAUSE ANALYSIS

Evaluate each of the following.

## A. Curriculum metadata insufficiency

Inspect whether relevant:

`curriculum_nodes.csat_importance`

values are:

* missing
* defaulted
* too sparse
* not evidence-based
* not actually used by the Stage 3 scoring pipeline

Report exact affected node codes.

Do not fill values yet.

---

## B. Prerequisite graph insufficiency

Inspect:

`curriculum_prerequisites`

for the current Algebra pilot.

Determine:

* whether important prerequisite relationships are missing
* whether Family centrality is underestimated because graph depth is incomplete
* whether prerequisite relationships are actually consumed by Stage 3 calibration

Report missing/weak relationships separately from confirmed relationships.

---

## C. Stage 2 Family taxonomy quality

Inspect all `지수함수와 로그함수` Family candidates.

Determine whether Families are:

* too fragmented
* too broad
* duplicates
* superficial surface variants
* missing a core reasoning pattern
* incorrectly mapped to curriculum Skills

Specifically detect:

### Over-splitting

multiple Families using the same meaningful solving structure.

### Under-splitting

one Family containing multiple genuinely distinct solving structures.

### Missing family

an official Gold reasoning pattern with no Stage 2 Family candidate.

Do not modify taxonomy during diagnosis.

---

## D. Gold Set mapping weakness

Inspect the actual 2028 Gold items relevant to:

* 지수
* 로그
* 지수함수
* 로그함수

Verify that:

* the correct Gold items were included
* curriculum mapping is correct
* reasoning signatures were extracted correctly
* Family evidence links are complete
* DIRECT/PARTIAL/COMPOSITE classification is justified

Check whether Family relevance is low because valid Gold evidence was simply not linked.

---

## E. Historical KICE evidence weakness

Inspect compatible historical KICE items.

Determine whether:

* too few were included
* compatible items were incorrectly classified
* useful evidence was excluded as UNCERTAIN
* incompatible legacy items were correctly excluded

Do NOT increase relevance by admitting curriculum-incompatible questions.

---

## F. Scoring-model behavior

Inspect the actual Stage 3 scoring formula.

Determine:

* whether any input is always null/zero
* whether Gold evidence is weighted correctly
* whether reference evidence is overweighted
* whether thresholds are internally consistent
* whether a Family can mathematically reach APPROVED under current data
* whether score normalization has a bug
* whether confidence and relevance are being conflated
* whether REVIEW_REQUIRED is triggered correctly

Do NOT change thresholds yet.

First calculate:

`maximum realistically attainable score`

for the strongest current Family using existing valid evidence.

---

# 3. ROOT-CAUSE REPORT

Before proposing implementation, the plan-agent must produce:

## Stage 3.5 Root Cause Report

For each category:

* Curriculum metadata
* Prerequisite graph
* Stage 2 taxonomy
* Gold mapping
* Historical KICE evidence
* Scoring model

classify:

`PRIMARY CAUSE`

`CONTRIBUTING CAUSE`

`NOT A CAUSE`

`NOT VERIFIED`

Include concrete evidence.

Then rank recommended fixes by:

1. evidence impact
2. correctness
3. implementation cost
4. risk of artificially inflating relevance

---

# 4. FIX PRINCIPLE

Only repair evidence/data that is genuinely incomplete or incorrect.

Do NOT:

* lower APPROVED threshold merely to pass
* promote REVIEW_REQUIRED manually
* change LOW to HIGH without evidence
* fabricate csat_importance
* add fake prerequisite relationships
* create Families solely to improve coverage metrics
* count MathJK reference frequency as official CSAT evidence
* include incompatible historical questions
* modify scores until a desired number of APPROVED Families appears

The target is:

`evidence-backed approval`

not:

`APPROVED count > 0 at all costs`

---

# 5. CURRICULUM IMPORTANCE REINFORCEMENT

If diagnosis shows missing/incomplete:

`curriculum_nodes.csat_importance`

reinforce only nodes relevant to:

`대수 > 지수함수와 로그함수`

Use explicit evidence hierarchy:

1. official curriculum centrality
2. official 2028 Gold Set
3. compatible historical KICE evidence
4. prerequisite centrality

Do not derive csat importance mainly from MathJK frequency.

Store:

* value
* evidence source
* calibration version
* rationale/confidence

if the existing architecture supports provenance.

Prefer auditable metadata over naked numeric values.

---

# 6. PREREQUISITE REINFORCEMENT

If diagnosis confirms missing prerequisite links:

add only evidence-supported relationships.

Examples may include relationships among:

* exponent laws
* exponential expressions
* logarithm definition
* logarithm properties
* exponential functions
* logarithmic functions
* equations/inequalities
* graph interpretation

Do not assume these examples must all be added.

Use current curriculum evidence.

Each added relationship should be explainable.

Re-run:

* self-reference checks
* duplicate checks
* cycle detection

---

# 7. STAGE 2 TAXONOMY REPAIR

If Stage 2 Family structure is found defective:

make the smallest justified repair.

Possible actions:

`MERGE`
`SPLIT`
`REMAP`
`ADD_GAP_CANDIDATE`
`REJECT`

Every change must preserve lineage to Stage 2 evidence.

For each taxonomy change store/report:

* previous Family code(s)
* resulting Family code(s)
* reason
* supporting evidence
* affected curriculum nodes
* affected Gold evidence
* status

Do not destroy historical records if current schema supports superseding/versioning instead.

---

# 8. GOLD MAPPING REPAIR

If Gold evidence mapping is incomplete or wrong:

repair:

* curriculum mapping
* skill overlap
* reasoning overlap
* match_type
* evidence weight

Use structural reasoning similarity.

Do NOT use wording similarity as primary evidence.

A DIRECT match must mean the same meaningful mathematical solving structure.

A PARTIAL match must remain partial.

Do not promote PARTIAL to DIRECT merely to improve scores.

---

# 9. SCORING MODEL FIX

Only modify scoring code if the diagnosis identifies an actual defect or structural bias.

Valid examples:

* null field incorrectly treated as zero
* incorrect normalization
* wrong evidence weight applied
* duplicate evidence counted twice
* impossible APPROVED threshold due to formula bug

Invalid reason:

"no Family passed."

Before and after any scoring change, produce a comparison report.

For each affected Family:

* old score
* new score
* exact cause of change
* evidence unchanged/changed
* relevance level before/after

No unexplained jumps.

---

# PHASE B — DEVELOPMENT AGENT

After plan review:

invoke:

`development-agent`

with only the approved fixes.

Explicitly authorize:

* curriculum metadata corrections
* prerequisite additions
* Stage 2 taxonomy repair if justified
* Gold evidence mapping correction
* KICE evidence correction
* scoring bug fixes if justified
* validation
* tests
* calibration rerun

Do NOT authorize:

* arbitrary threshold reduction
* manual Family approval
* Stage 4 templates
* problem generation
* mass PDF analysis
* unrelated refactoring
* production Supabase push

---

# 10. RECALIBRATION

After fixes:

rerun the Stage 3 calibration pipeline for:

`대수 > 지수함수와 로그함수`

Do not manually edit the outcome.

Produce results:

## Family Status

* APPROVED
* REVIEW_REQUIRED
* REJECTED

## Relevance

* CORE
* HIGH
* MEDIUM
* LOW
* REJECT

## Evidence

For each top Family:

* Gold evidence count
* historical compatible evidence count
* curriculum centrality
* reasoning evidence
* confidence

## Coverage

* Gold Family Coverage
* Gold Skill Coverage
* Family Gap Count
* Unsupported Family Rate

Compare:

`Before Stage 3.5`
vs
`After Stage 3.5`

---

# 11. IMPORTANT OUTCOME RULE

It is acceptable for Stage 3.5 to finish with:

`APPROVED = 0`

if the evidence genuinely does not support approval.

If that happens:

do NOT lower standards.

Instead identify whether the next correct action is:

* expand Stage 2 pilot coverage
* acquire more official compatible evidence
* revise taxonomy
* keep current Families REVIEW_REQUIRED

The pipeline must prefer:

`honest zero`

over:

`false approval`

---

# 12. VALIDATION

Run checks for:

* duplicate curriculum nodes
* invalid csat_importance
* unsupported importance values
* prerequisite cycles
* duplicate prerequisites
* Family lineage integrity
* duplicate evidence links
* incompatible historical evidence affecting score
* invalid Gold mapping
* score calculation reproducibility
* manual status overrides
* threshold changes without justification
* MathJK accidentally treated as Gold evidence

---

# PHASE C — STAGE QA AGENT

After development and recalibration:

invoke:

`stage-qa-agent`

Completed stage:

`PocketQ Problem DB Stage 3.5 — Calibration Recovery & Evidence Reinforcement`

The Stage QA Agent must independently verify:

1. root cause was identified from actual data
2. APPROVED status was not manually forced
3. thresholds were not arbitrarily lowered
4. csat_importance values are evidence-supported
5. prerequisite additions are valid
6. taxonomy changes are justified
7. Gold evidence outranks MathJK evidence
8. incompatible KICE items do not affect calibration
9. scoring changes, if any, fix a real issue
10. before/after score differences are explainable
11. recalibration is reproducible
12. existing PocketQ functionality has no regression
13. Stage 4 has not started

Stage QA must manually inspect at least:

* strongest APPROVED Family if one exists
* strongest REVIEW_REQUIRED Family
* one rejected Family
* one taxonomy merge/split if performed
* one Gold evidence mapping
* one csat_importance reinforcement
* one prerequisite reinforcement

If actual verification is impossible:

report:

`NOT VERIFIED`

Do not infer PASS.

---

# 13. AUTOMATED VERIFICATION

Stage QA must independently run applicable:

* typecheck
* lint
* tests
* build
* curriculum validation
* prerequisite validation
* Stage 3 calibration tests
* Stage 3.5 focused tests

Do not modify tests to make them pass.

---

# 14. DEFECT LOOP

If Stage QA returns:

`STAGE CONDITIONAL PASS — FIX REQUIRED`

or:

`STAGE FAIL — DO NOT PROCEED`

do NOT start Stage 4.

Implementation defects:
→ `development-agent`

Architecture/calibration defects:
→ `plan-agent`
→ `development-agent`

Then rerun:

`stage-qa-agent`

Only a fresh:

`STAGE PASS — READY FOR NEXT STAGE`

completes Stage 3.5.

---

# DEFINITION OF DONE

Stage 3.5 is complete only when:

1. the reason for APPROVED=0 is explicitly understood
2. only genuine missing/incorrect evidence was repaired
3. curriculum importance is evidence-backed
4. prerequisite graph is valid
5. Stage 2 taxonomy defects are repaired if required
6. Gold mapping is complete and structurally justified
7. KICE evidence is curriculum-filtered
8. scoring behavior is explainable
9. no manual Family approval occurred
10. no arbitrary threshold reduction occurred
11. Stage 3 recalibration was rerun
12. before/after results are documented
13. existing PocketQ functionality has no regression
14. Stage QA returns:

`STAGE PASS — READY FOR NEXT STAGE`

Stop after Stage 3.5.

Do NOT automatically begin Stage 4.

---

# FINAL REPORT

Return:

## PocketQ Problem DB Stage 3.5 Report

### Root Cause

* primary cause
* contributing causes

### Curriculum Reinforcement

* nodes changed
* importance evidence

### Prerequisite Reinforcement

* relationships added/changed

### Taxonomy

* merges
* splits
* remaps
* gaps

### Gold/KICE Evidence

* mappings corrected
* compatible evidence added/removed

### Scoring

* formula changes, if any
* justification

### Recalibration

Before:

* APPROVED
* REVIEW_REQUIRED
* REJECTED

After:

* APPROVED
* REVIEW_REQUIRED
* REJECTED

### Coverage

* Gold Family Coverage
* Gold Skill Coverage
* Family Gap Count

### Automated Verification

* typecheck
* lint
* tests
* build

### Stage QA

* issues
* NOT VERIFIED
* final decision

Final output:

`PASS — READY TO RETRY STAGE 4`

only if Stage QA returned:

`STAGE PASS — READY FOR NEXT STAGE`
